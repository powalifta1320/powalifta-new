# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Local tooling (Claude preview server — not part of the app)
.claude/

# Logs / temp
*.log
.cache/
tmp/

# Env / secrets (none currently — the Supabase anon key in db.js is intentionally public)
.env
.env.*

# AI assistant — local-only dev tool (ai-chat branch). The local config holds a
# real Anthropic key and must never be committed or deployed. The .example is safe.
ai-config.local.js
