# POWALIFTA — context for Claude Code

POWALIFTA is a powerlifting platform — RPE-native programming, automatic e1RM tracking, free for athletes, paid tiers for coaches with rosters past 3 athletes. Tagline: **Built for the barbell.**

This file gets you up to speed fast. Read it before changing anything.

## Stack

- **Hosting:** Vercel (custom domain `powalifta.com`, `www` canonical)
- **Backend:** Supabase (Postgres + Auth + Edge Functions). Project URL is in `db.js`.
- **Payments:** Lemon Squeezy (handles subscriptions + marketplace as Merchant of Record)
- **Email:** Resend (transactional via edge functions)
- **Analytics:** Plausible (`powalifta.com`, GDPR-compliant, no cookie banner needed)
- **Error tracking:** local `error-log.js` + optional Sentry/Supabase forwarding
- **No build step.** Vanilla JS, plain HTML, hand-written CSS. Service Worker (`sw.js`) is **network-first**: online always fetches fresh (so a deploy is never stale), every successful same-origin GET is mirrored into the `powa-v2` cache, and the cache is read only when the network fails — so the app opens offline. `sw.js` also hosts the Web Push `push` + `notificationclick` handlers.
- **Push:** Web Push via VAPID. `push_subscriptions` table + `send-push` edge function. Client lives in `app.js` (`PowaPush` module). iOS Web Push only fires for a Home-Screen-installed PWA on iOS 16.4+.

## Architecture

Each top-level page is its own HTML file with its own inline `<script>` glue at the bottom:

- `index.html` — landing page (hero + features + how-it-works + coaches + marketplace teaser + pricing + CTA band)
- `athlete.html` — athlete dashboard (Today / Full program / Progress / Bodyweight / Coach / Library / Marketplace tabs)
- `coach.html` — coach dashboard (Roster / Program builder / Templates / Marketplace listings / Progress / Notes inbox)
- `coach-profile.html` — public coach profile (renders from URL param `?id=`)
- `marketplace.html` — public program marketplace (filters + search + buy flow)
- `admin.html` — admin dashboard (users, sales, invites, logs — restricted to `is_admin`)
- `about.html`, `faq.html`, `privacy.html`, `terms.html`, `reset-password.html`, `404.html` — static content
- `leaderboard.html` — public opt-in DOTS leaderboard (`renderLeaderboard` in app.js; sex filter + sign-in/up CTA)
- `guides.html` — SEO guide hub (`.guide-hub`; links the 4 articles below), plus `guide-rpe.html`, `guide-e1rm.html`, `guide-dots.html`, `guide-peaking.html` — long-form theory articles (`.guide-wrap`/`.prose`; each carries `Article` + `BreadcrumbList` JSON-LD; math mirrors `app.js`). All in `sitemap.xml`; every public footer cross-links **Guides**.
- `index.html` and the dashboards all share `app.js` (~2100 lines: store, modals, signup/login, plate calc, RPE math, charts, programming helpers) and `db.js` (~900 lines: Supabase client + mappers).

### Animation / FX layer (drop-in modules)

Loaded after `styles.css` on the marketing pages. Each is additive and removable:

| File | Purpose |
|---|---|
| `hero-fx.css/js` | Hero motion: gradient mesh, drifting grid, chalk-dust particles, dot pulse ring, headline shimmer, accent breathing glow, chart card glint, +18% pill pulse + counter tickup |
| `scroll-fx.css/js` | Declarative scroll reveals via `data-fx="up\|down\|left\|right\|scale\|blur"` + `data-fx-stagger`, `data-fx-count`, `data-fx-parallax`. IntersectionObserver-based. |
| `ui-fx.css/js` | Global polish: red selection color, smooth scroll, button hover glow + ripple, card hover lift, link underline grow, marquee edge fade + pause-on-hover, sticky nav blur on scroll, red focus rings, FOR COACHES pitch box styling |
| `insane-fx.css/js` | Signature moments: magnetic primary CTAs, hero headline scramble reveal on first paint. (Custom cursor + 2D scroll-barbell loader exist as dormant code but are no longer wired in — user rejected both.) |
| `three-fx.css/js` | DORMANT — Three.js 3D barbell in the hero with plate loading on scroll. User rejected, includes removed from `index.html`. Files left in repo as inert. |
| `error-log.js` | Captures `window.onerror` + `unhandledrejection`. Keeps last 50 in localStorage (`window.__powaErrors()` inspects). Optional Sentry / Supabase edge function forwarding via the two constants at the top of the file. |

Every fx file respects `prefers-reduced-motion`.

## Brand tokens

- Background: `#0b0b0c`
- Primary red: `#ff2d3f` (deep variant `#b71629`)
- Headlines: `Anton` (display)
- Body: `Plus Jakarta Sans` (400/500/600/700/800)
- Chart labels / monospace: `Space Grotesk`
- ALL CAPS for section eyebrows, headlines, hero stats; sentence case for body copy

## Core math

- **e1RM** uses RPE-percentage / Tuchscherer-style formula (`calcE1RM` in `app.js` ~line 1530):
  `e1RM = weight / (1 - ((reps - 1) + (10 - RPE)) * 0.0333)` (floored at `weight / 0.4`)
- **Variant multipliers** (`VARIANTS` object in `app.js` ~line 1499, applied by `multiplierFor()` ~line 1521): each main lift has several variants, each with its own multiplier. **Competition is the 1.0 baseline**; harder variants scale e1RM *up* so the chart compares competition-equivalent (e.g. squat Tempo 1.12, bench Incline 1.12, deadlift RDL 1.10). Variant counts: squat 4, bench 5, deadlift 4.
- **Plate calc** is in `athlete.html`. Tap any displayed weight → modal with plate breakdown.

## Critical fixes — do NOT regress

1. **Rest day in coach.html.** Three pieces around lines 634, 662, 880: "Add rest day" button, `isRestDay` heuristic (regex `/\b(rest|off|recovery)\b/i`), `addRestDay()` function. If you refactor the program builder, keep these alive.

2. **Pin re-render in athlete.html.** `pinDayToDate()` and `unpinDay()` both call `populateDaySelect()`, `refreshPinButtons()`, AND `renderToday()`. Without that third call the data saves but the main session panel doesn't refresh — looks broken.

3. **Demo mode** (`?demo=1`) sets `window._demoMode = true`. `persistProgram` and other write paths short-circuit on it. Don't break that — it's how the public hero demo works.

4. **CSP must allow same-origin framing.** `vercel.json`'s `Content-Security-Policy` keeps `frame-ancestors 'self'` and `frame-src 'self' https://*.lemonsqueezy.com` — NOT `'none'`. The in-app test harness nests same-origin iframes (`admin.html` → `tests.html` → `athlete.html?demo=1`); `'none'` (or a `frame-src` without `'self'`) makes the deployed site show "This content is blocked." `'self'` still blocks all cross-origin framing, so clickjacking protection is intact. Only bites on deploy — local static preview sends no CSP. Mirror any change in `docs/vercel-headers-snippet.json`.

## Roles & data model (Supabase)

- `profiles` — every user. `userType` is `athlete` | `coach` | `admin`. `coach_id` on an athlete row links them to a coach. Setting `coach_id = null` disconnects. `subscription_tier` is `free` | `basic` | `pro` | `premium` (athlete-limit 3/10/25/unlimited — see `migration-tier-enforcement.sql`); the LS subscription webhook writes it plus `subscription_status` / `ls_subscription_id` / `ls_customer_id` / `subscription_updated_at` (created by `migration-subscription-columns.sql`). `is_admin` boolean gates the admin dashboard. All three privileged columns are frozen against client self-escalation by `migration-profiles-privilege-guard.sql`.
- `programs` — coach-built week/day/exercise/sets per athlete. `athlete_id` + `coach_id`.
- `templates` — coach's reusable program payloads.
- `workout_logs` — every set logged (client-side: `workoutLogs`, mapped in db.js). Drives e1RM. Older notes sometimes say "logs" — the real table is `workout_logs`.
- `bodyweight` — daily weigh-ins.
- `goals` — SBD + bodyweight targets.
- `marketplace_programs` — public listings.
- `program_sales` — marketplace purchase records, with `coach_payout_cents` + `payout_status` (`pending` | `paid` | `cancelled`). `cancelled` = refunded/voided by the `order_refunded` webhook path. Idempotency keys `ls_order_id` + `ls_event_id` (both UNIQUE).
- `invites` — 6-char codes a coach generates to connect athletes.
- `session_notes` + `checkins` — coach feedback + weekly check-in form.
- `client_errors` — populated by `send-client-error` edge fn.
- `program_reviews` — marketplace ratings/reviews. Public read; verified-buyer writes (RLS `EXISTS` against `program_sales`). Denormalised `coach_id` + `buyer_name` so anon viewers don't read `profiles`.
- `messages` — coach↔athlete direct messages. `(coach_id, athlete_id)` is the thread; `sender_id` is who wrote it; `read_at` drives unread badges. Member-only RLS; insert gated on a live coach→athlete link.
- `form_checks` — athlete form-check video requests + coach reply. Video bytes live in the private `form-checks` Storage bucket (not this table); `storage_path` points at them.

All tables have RLS. Anon key in `db.js` is intentionally public (Supabase pattern). **Security depends entirely on RLS policies being correct.**

## RLS gotcha worth verifying

The new coach-removes-athlete + athlete-leaves-coach features both call `DB.updateProfile(userId, {coach_id: null})`. The RLS policy on `profiles` must allow:

- A coach to update `coach_id` to null on profiles where the existing `coach_id = auth.uid()`
- An athlete to update their own `coach_id` to null where `id = auth.uid()`

If the policy is "users can only update their own profile," the coach-remove path silently fails (no error, no toast). Worth checking + writing a permissive-enough policy.

## Edge functions

In `supabase/functions/<name>/index.ts` (Supabase CLI standard layout — deployed by name). **Deploy them + push secrets with `./deploy.sh`** (uses `npx supabase`, no global install; one-time `supabase login` + `supabase link --project-ref cxnotrikxvzncupswvio` first). The script already encodes the correct per-function Verify-JWT setting (the `--no-verify-jwt` group vs the JWT-required group), and pushes every secret from your local `.env` — see `.env.example` for the manifest of what key each function needs. Dashboard deploys still work as a fallback.

- `ls-webhook.ts` — Lemon Squeezy subscription webhook → updates `profiles.subscription_tier`. Env: `LEMON_SQUEEZY_WEBHOOK_SECRET`. Verify JWT OFF.
- `ls-marketplace-webhook.ts` — Lemon Squeezy marketplace webhook → records sale + grants program access. Env: `LEMON_SQUEEZY_WEBHOOK_SECRET`. Verify JWT OFF.
- `send-welcome.ts` — branded welcome email after signup. Env: `RESEND_API_KEY`. Verify JWT OFF.
- `send-client-error.ts` — NEW. Receives client-side error reports. Env: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`. Verify JWT OFF.
- `send-program-assigned.ts` — NEW. Sends email to athlete when coach hits the "📧 Notify" button in the program builder. Env: `RESEND_API_KEY`. Verify JWT **ON** (only logged-in coach can fire).
- `send-invite.ts` — NEW. Auto-emails a prospective athlete ("X coach invited you") the moment a coach generates an invite code. The **code + coach name are resolved server-side** from the caller's own newest UNUSED `invites` row (`coach_id = caller` + `ilike email`), so a caller can only ever email an invite they actually made (anti-phishing gate, same spirit as `send-program-assigned`). Env: `RESEND_API_KEY` + `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`. Verify JWT **ON**. Client calls it fire-and-forget through `DB.sendInvite(email)` from `generateInvite()` (coach.html); the visible "📧 Send via email" mailto stays as a manual fallback, and demo mode skips the network. No new table/migration (uses existing `invites`).
- `send-push.ts` — NEW. Delivers a Web Push to every device a user registered (`push_subscriptions`). Caller is identified from their JWT and may push only to themselves OR the other party of a live coach↔athlete link (same gate as `messages` RLS); recipient's tokens read via service role, dead 404/410 endpoints pruned. Env: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`. Verify JWT **ON**. Client calls it through `DB.sendPush()`. **To bring live:** `npx web-push generate-vapid-keys`, paste the *public* key into `VAPID_PUBLIC_KEY` at the top of `app.js`, set the keypair (+subject) as function secrets, run `migration-push-subscriptions.sql`, deploy. Until the public key is set, the in-app "🔔 Enable alerts" toggle degrades to a toast — nothing breaks.
- `send-weekly-digest.ts` — NEW. **Cron-triggered batch** (NOT browser-called) that emails every opted-in athlete a last-7-days recap: sessions, sets, tonnage, best `e1rm_comp` per main lift, current competition-equivalent total (sum of best `e1rm_comp` over a 90-day window), and a bodyweight trend. Aggregates **stored `e1rm_comp`** straight from `workout_logs` — no e1RM math reimplemented in Deno. Audience = `email_preferences.weekly_digest = true` JOIN `profiles` (athletes with an email); athletes with **zero sessions in the last 7 days are skipped** (no "you did nothing" email). Every email carries a one-click unsubscribe link (`UNSUB_BASE` + `unsub_token`, groundwork for #36). Env: `RESEND_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET` (required), `UNSUB_BASE` (optional). **Verify JWT OFF** — a cron scheduler has no session, so it **fails closed on a shared secret**: caller must send `Authorization: Bearer <CRON_SECRET>` (or `x-cron-secret`); with no `CRON_SECRET` configured it refuses to run. `POST {dryRun:true}` returns the audience + computed recaps without sending. One bounded 90-day read of logs+bodyweight for the whole audience, grouped in memory (no per-athlete round-trips; caps documented in-file for a future move to an aggregating SQL view/RPC). Client side: `DB.getEmailPrefs`/`DB.updateEmailPrefs` + the Settings-modal toggles are the opt-out surface. **PREP — cannot fire until deployed + secrets set + a cron entry exists** (the pg_cron/`net.http_post` snippet is in the function header; Mondays 14:00 UTC).
- `ai-chat.ts` — NEW. Secure proxy for the AI assistant. Holds `GEMINI_API_KEY` server-side and forwards the browser's question + data snapshot to Google's Gemini (`gemini-2.5-flash`). Env: `GEMINI_API_KEY` (required), `AI_DAILY_CAP` (optional, default 20). Verify JWT **ON**. Maps our `user`/`assistant` turns to Gemini's `user`/`model` + `system_instruction` shape, clamps `maxOutputTokens`/system length/history server-side, and enforces a per-user daily cap via `ai_chat_usage` (service role). The browser calls it through `DB.aiChat()` (`sb.functions.invoke`), so no key is ever client-side. Provider is swappable in one file — only this function knows it's Gemini. (Note: Gemini's free tier returns a hard `429 "check your plan and billing"` for this account — billing must stay enabled on the Google Cloud project. Was briefly swapped to Groq during the billing outage; reverted once billing went through, since Gemini 2.0 Flash is ~4–6× cheaper per token.)
- `delete-account.ts` — NEW. Self-serve account deletion / GDPR erasure (#33). `POST {}` with **no body** — the account to erase is the CALLER, identified ONLY from the session JWT, so a caller can never delete anyone but themselves. Service role then (1) best-effort Storage sweep of `<uid>/` in the private `form-checks` bucket + public `avatars` bucket (deleting the auth user does NOT remove Storage objects), then (2) `auth.admin.deleteUser(uid)` — one call that CASCADES across every `ON DELETE CASCADE` FK to profiles(id)/auth.users(id) (profile, logs, programs, bodyweight, goals, meets, messages, program_reviews, referrals, leaderboard_entries, form_checks, session_notes, checkins, coach_requests, email_preferences, push_subscriptions, ai_chat_usage, marketplace_programs). `program_sales` are RETAINED (tax records) — `migration-account-deletion.sql` switches their buyer_id/coach_id/marketplace_program_id FKs to ON DELETE SET NULL so each sale survives with its identity nulled. Rate-limited (dogfoods the #26 helper, uid bucket `delete-account` 5/hr). Env: `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (auto-injected — **no new secret**). Verify JWT **ON**. Client calls it via `DB.deleteAccount()` (unwraps the FunctionsHttpError JSON like `sendInvite`); the Settings → Danger zone "Delete my account" opens a type-`DELETE`-to-confirm modal (`deleteMyAccount()` in app.js) that offers Export-first, and on server failure does NOT sign you out — it shows the real reason + the manual email fallback. **PREP — enable = run `sql/migration-account-deletion.sql` then `./deploy.sh` (delete-account is in the WITH_JWT group).** Until deployed, the modal's `DB.deleteAccount()` invoke fails and the honest fallback toast points at the email path; `?demo=1` short-circuits with a toast (no network).

- `ls-portal.ts` — NEW. In-app subscription management (#34). `POST {}` with **no body** — the subscription to manage is the CALLER's own. Returns a **signed, single-use Lemon Squeezy customer-portal URL** so an existing subscriber can update their card / switch / pause / cancel without the LS API key ever reaching the browser. Anti-phishing gate (same spirit as `send-invite`): **uid comes ONLY from the JWT**, and the caller's `ls_subscription_id` is resolved **server-side from their own `profiles` row** via service role → LS API `GET /v1/subscriptions/{id}` → `data.attributes.urls.customer_portal`. Rate-limited (#26 helper, uid bucket `ls-portal` 30/hr). Env: `LEMON_SQUEEZY_API_KEY` (**optional** — the ONE new secret) + auto-injected `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`. Verify JWT **ON**. **Graceful degradation:** every "can't sign a link" branch (no key / no subscription id / LS error) returns **HTTP 200 `{ok:false,reason}`** (not an error), so the client silently falls back to the LS no-code email-magic-link portal. Client calls it via `DB.subscriptionPortal()`; `openBillingPortal()` (app.js) opens the tab synchronously then upgrades the URL to the signed link if present.

The signature verification in both LS webhooks uses HMAC-SHA-256. The old length pre-check that leaked length has been replaced with a `timingSafeEqual` helper (double-HMAC with a single-use random key → constant-time, no length side-channel) in both `ls-webhook.ts` and `ls-marketplace-webhook.ts`.

## SQL migrations

In `sql/`. Apply via Supabase SQL Editor in this order:

```
migration-coach-profile.sql
migration-feedback-checkins.sql
migration-self-coached.sql
migration-marketplace.sql
migration-marketplace-1rm.sql
migration-directory-hidden.sql
migration-subscription-columns.sql    # guarantees profiles' subscription_* + ls_* cols exist (LS sub-webhook writes them)
migration-tier-enforcement.sql
migration-client-errors.sql
migration-profiles-rls.sql            # UPDATE policies for coach/athlete disconnect
migration-profiles-privilege-guard.sql # freezes subscription_tier/user_type/is_admin vs client self-escalation (run AFTER profiles-rls)
migration-claim-invite-reconnect.sql  # recreates the `claim_invite` SECURITY DEFINER RPC so an athlete who left a coach can join a new one (overwrites coach_id unconditionally). Bug-fix for the ad-hoc live RPC; needs profiles + the invites table. Run AFTER profiles-rls. Idempotent/independent.
migration-coach-requests.sql          # coach_requests table + accept_coach_request()/decline SECURITY DEFINER (coachless athlete asks a coach to take them on; coach Accepts → writes profiles.coach_id). athlete_name denormalized (coach can't read an unlinked athlete's profile). Needs profiles. Run AFTER profiles-rls, BEFORE migration-rls-hardening (which hardens coach_requests).
migration-marketplace-reviews.sql     # program_reviews table + RLS (verified-buyer writes)
migration-messages.sql                # coach↔athlete direct messaging + RLS
migration-messages-realtime.sql       # adds `messages` to supabase_realtime publication (live threads/badges); run AFTER migration-messages
migration-form-checks.sql             # form_checks table + private `form-checks` Storage bucket + object policies
migration-form-checks-rls-fix.sql     # fixes coach playback: DROPs+recreates the `fc_obj_read` storage.objects policy so a coach can read a linked athlete's video without depending on brittle profiles-SELECT visibility. Run AFTER migration-form-checks. Idempotent.
migration-ai-chat-usage.sql           # ai_chat_usage table (per-user daily AI cap; RLS-locked to service role)
migration-push-subscriptions.sql      # push_subscriptions table + RLS (own-rows-only; send-push reads via service role)
migration-referrals.sql               # referral_code/referred_by_code on profiles + referrals table + SECURITY DEFINER attribution trigger + RLS (run AFTER profiles-rls + privilege-guard)
migration-referral-rewards.sql        # referral_reward_claims ledger + select-own RLS + SECURITY DEFINER claim_referral_reward(tier) earn-gate (run AFTER migration-referrals)
migration-marketplace-taxonomy.sql    # marketplace_programs.category (training-focus taxonomy, distinct from tier/length) + index; nullable, no CHECK (keys ship client-side)
migration-meets.sql                   # meets table (finished-meet OUTCOMES: best good lift per discipline) + RLS (own-rows-only + coach read-only via live link). Additive/independent — apply any time.
migration-leaderboard.sql             # leaderboard_entries (denormalized opt-in DOTS board: row existence IS the opt-in) + public-read RLS + owner-only insert/update/delete (run AFTER migration-profiles-rls)
migration-email-preferences.sql       # email_preferences (weekly_digest + product_emails + UNIQUE unsub_token) + own-row RLS + touch trigger + profiles backfill. Backs #19 (weekly digest opt-out) AND #36 (one-click unsubscribe). Additive/independent — apply any time.
migration-notification-prefs.sql      # adds push_messages/push_form_checks/push_program bool cols (default true) to email_preferences for #30 per-category push muting. No new RLS (inherits email_preferences own-row policies; send-push reads via service role). Run AFTER migration-email-preferences. Idempotent; send-push FAILS OPEN if the cols aren't there yet, so deploy order doesn't matter.
migration-client-errors-admin-read.sql # admin-only SELECT policy (is_admin()) on client_errors so the admin Errors tab can read forwarded rows. Writes stay service-role only. Run AFTER migration-client-errors. Additive/independent.
migration-edge-rate-limits.sql        # edge_rate_limits table (service-role only, no policies) + atomic rl_bump(bucket,identity,window_secs,limit) RPC (fixed-window counter) for edge-function abuse guards. Idempotent/independent — apply any time. Every caller FAILS OPEN if the RPC isn't there yet, so deploy order never matters.
migration-account-deletion.sql        # makes self-serve erasure (#33) cascade-safe: program_sales buyer_id/coach_id/marketplace_program_id → DROP NOT NULL + ON DELETE SET NULL (retain the tax record, null the identity) so deleting a user/coach/listing no longer hits a RESTRICT FK; plus a guarded DO block that upgrades profiles→auth.users to ON DELETE CASCADE iff it isn't already. Idempotent/order-independent. Run BEFORE deploying delete-account (until then deleteUser fails cleanly and the client shows the manual email fallback).
migration-rls-hardening.sql           # RUN LAST — security-audit hardening pass (#24). Column-level immutability triggers + tightened policies across profiles (coach-detach guard), messages, session_notes, coach_requests, marketplace_programs (self-publish guard), form_checks, and storage.objects (avatars owner-update). Recreates accept_coach_request → depends on migration-coach-requests. Idempotent (CREATE OR REPLACE / DROP IF EXISTS); apply after every table/policy it references exists.
migration-rls-hardening-2.sql         # RUN AFTER rls-hardening (audit pass #2). Fixes: (HIGH) program_reviews INSERT/UPDATE now bind the client-supplied denormalized coach_id to the program's REAL coach (marketplace_programs join) so a buyer can't inject a review onto a rival coach's public rating; (MED) pins `SET search_path = public` on the SECURITY DEFINER mp_prevent_self_publish() (was the only DEFINER fn missing it). Idempotent; verification queries in the file header. Depends on migration-marketplace-reviews + migration-rls-hardening.
```

**Storage note:** `migration-form-checks.sql` also creates a private Storage bucket
(`form-checks`, 100 MB/file cap, video MIME types) and RLS policies on
`storage.objects`. Athlete videos live at `<athlete_id>/<ts>-<name>`; the leading
folder = owner uid, which the object policies key off. Playback uses short-lived
signed URLs (`DB.formCheckSignedUrl`), never public links.

## Go-live checklist (deploy + secrets runbook)

Single source of truth for taking the whole backlog live. The site itself (static HTML/JS/CSS) is already live on Vercel on every push — this covers the **backend** pieces the PREP items need. Nothing here handles secrets for you; fill `.env` yourself and run the scripts.

**1 — DB migrations (do first).** Run every file in `sql/` via the Supabase SQL Editor, in the exact order documented in "SQL migrations" above. All are idempotent + additive; re-running is safe. Most features degrade gracefully (client fails open) until their migration lands, so partial application never breaks the site.

**2 — Deploy edge functions.** One-time: `supabase login` + `supabase link --project-ref cxnotrikxvzncupswvio`. Then `./deploy.sh` (deploys all 12 functions with the correct per-function Verify-JWT setting **and** pushes every secret from `.env`). Sub-commands: `./deploy.sh functions` (deploy only), `./deploy.sh secrets` (push secrets only). Dashboard deploys work as a fallback.

**3 — Secrets → which feature each gates** (fill in `.env` from `.env.example`, then `./deploy.sh secrets`):

| Secret | Gates | If unset |
|---|---|---|
| `LEMON_SQUEEZY_WEBHOOK_SECRET` | LS subscription + marketplace webhooks (tier writes, sale records) | webhooks reject → no tier/sale updates |
| `LEMON_SQUEEZY_API_KEY` | `ls-portal` signed billing deep-links (#34) | client falls back to the no-secret email-magic-link portal — fine |
| `RESEND_API_KEY` | all transactional email (welcome, invite, program-assigned, weekly digest) | emails no-op (client fire-and-forget; UI unaffected) |
| `CRON_SECRET` | `send-weekly-digest` auth gate (#19) | digest endpoint rejects the cron call |
| `GEMINI_API_KEY` and/or `GROQ_API_KEY` | live "Ask AI" (#—); set BOTH for Gemini→Groq fallback | assistant runs in local **mock** mode for everyone (graceful) |
| `VAPID_PRIVATE_KEY` + `VAPID_SUBJECT` | Web Push delivery (#30) | "🔔 Enable alerts" degrades to a toast; nothing breaks |

`SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` are auto-injected into every function (never set them; `deploy.sh` strips `SUPABASE_*` before pushing).

**4 — Client-side constants (non-secret, edited in source).**
- **VAPID public key** — already pasted at `app.js:628` (`VAPID_PUBLIC_KEY`, the public half of the send-push keypair). If you regenerate the keypair (`npx web-push generate-vapid-keys`), update BOTH this constant and the `VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` secrets.
- **`LIVE_BACKEND`** (`ai-chat.js:31`, default `true`) — master switch for the AI assistant; set `false` to force mock everywhere regardless of secrets.
- **`LS_BILLING_PORTAL`** (`app.js:1141`) — the no-secret LS billing fallback URL; only change if the store slug changes.

**5 — External dashboard wiring (outside this repo).**
- **Lemon Squeezy** → point two webhooks at the deployed function URLs (`ls-webhook`, `ls-marketplace-webhook`), signing secret = `LEMON_SQUEEZY_WEBHOOK_SECRET`.
- **Weekly digest cron (#19)** → schedule a weekly POST to the `send-weekly-digest` function URL with header `Authorization: Bearer <CRON_SECRET>` (Supabase scheduled function, GitHub Action, or any cron).
- **Google Cloud** → billing must stay enabled on the project behind `GEMINI_API_KEY` (free tier hard-429s this account); `GROQ_API_KEY` (free, no card) is the fallback.

**6 — Per-feature go-live status.** Every backlog feature is DONE in code; the PREP/gated ones need the row's action to switch from mock/degraded to live:

| Feature | To go live |
|---|---|
| AI assistant | secret (GEMINI and/or GROQ) + deploy `ai-chat` + `migration-ai-chat-usage.sql` |
| Web Push (#30) | `VAPID_PRIVATE_KEY`/`SUBJECT` secrets + deploy `send-push` + `migration-push-subscriptions.sql` (+ `-notification-prefs`) |
| Weekly digest (#19) | `RESEND_API_KEY` + `CRON_SECRET` secrets + deploy `send-weekly-digest` + `migration-email-preferences.sql` + schedule the cron |
| Unsubscribe (#36) | deploy `unsubscribe` + `migration-email-preferences.sql` |
| Account deletion (#33) | `migration-account-deletion.sql` (BEFORE) + deploy `delete-account` |
| Subscription portal (#34) | optional `LEMON_SQUEEZY_API_KEY` + deploy `ls-portal` (else email-link fallback) |
| Error forwarding (#25) | deploy `send-client-error` + `migration-client-errors.sql` (+ `-admin-read` for the admin tab) |
| Rate limiting (#26) | `migration-edge-rate-limits.sql` (callers fail open until then) |
| Everything else (client-only) | already live on Vercel push — no backend action |

## Recently shipped (overnight security + athlete-analytics batch)

- **Security hardening pass** — DONE (client live on push; edge fns need redeploy; 1 new migration). Two audit workflows (adversarial-verified) surfaced + fixed: **4 stored-XSS sinks** now `escHtml`/`escapeHtml`'d (admin approve/deliver/invites modals + the anon-facing `marketplace.html` program-detail "by <coach>" line); **6 demo-write leaks** gated on `window._demoMode` (athlete saveEditedLog/leaveMyCoach, coach removeAthleteFromRoster/notifyAthleteAssigned, app.js saveSettings/handleAvatarUpload); **5 edge fns hardened** (ai-chat + ls-webhook + ls-marketplace-webhook no longer leak stack/exception text; ls-webhook invalid-sig no longer leaks body length; send-weekly-digest now uses constant-time `timingSafeEqual` on CRON_SECRET; send-client-error caps body at 20KB pre-parse + validates `ts`); **`sql/migration-rls-hardening-2.sql`** (new, RUN LAST) binds `program_reviews.coach_id` to the program's real coach in RLS (was review-injection-able) + pins `search_path` on `mp_prevent_self_publish`. `error-log.js` `SUPABASE_ERROR_ENDPOINT` set live. Signup got a pure `pwdStrength()` meter. Full triage + morning actions in `OVERNIGHT.md`.
- **Athlete analytics (pure helpers, all unit-tested, all on the Progress tab / strength card)** — DONE. `ipfGlPoints()` (official IPF GoodLift score beside DOTS in the strength card); `deloadSignal()` (amber "deload might be due" nudge after 3 rising-load weeks at RPE≥8); `trainingHeatmap()` (GitHub-style 12-week calendar, red intensity ramp); `prTimeline()` (chronological e1RM-PR feed, newest first, +kg jump badges); `trainingAchievements()` (weekly streak + session/PR milestone badge strip); session tonnage ("N kg moved") in the Today summary; PR haptic (`navigator.vibrate`, feature-detected). All in `app.js` (window-exported), rendered in `athlete.html`, styled in `styles.css`, demo-safe. tests.html 143→171 green.

## Recently shipped (reviews / messaging / form checks)

- **Marketplace reviews/ratings** — DONE. `program_reviews` table (`migration-marketplace-reviews.sql`), public read, verified-buyer writes (RLS `EXISTS` against `program_sales`), one review per buyer per program (upsert). Shared helpers in `app.js`: `reviewStats()`, `starString()`, `starsHtml()` (unit-tested in `tests.html`). UI: rating badges on `marketplace.html` browse cards, summary + list + gated write-form on the detail page, aggregate rating + testimonials section on `coach-profile.html`. db.js: `listProgramReviews`, `listReviewsForPrograms`, `listReviewsForCoach`, `hasPurchasedProgram`, `upsertProgramReview`, `deleteProgramReview`.
- **Coach ↔ athlete messaging** — DONE. `messages` table (`migration-messages.sql`), member-only RLS, insert gated on a live coach→athlete link. Shared UI `renderMessageThread()` in `app.js` (bubbles + composer, Enter sends). Athlete: Coach tab thread. Coach: 💬 button per roster card → `messageModal`, unread badges via `DB.listUnreadForUser`. db.js: `listMessages`, `sendMessage`, `markThreadRead`, `listUnreadForUser`.
- **Form-check video uploads** — DONE. `form_checks` table + private Storage bucket (`migration-form-checks.sql`). Athlete uploads a clip (Coach tab → Form checks) with optional lift + note; coach sees a Form-checks inbox on the roster, opens `formCheckModal` to watch (signed URL) + write a cue. db.js: `uploadFormCheck`, `listFormChecksForAthlete`, `listFormChecksForCoach`, `formCheckSignedUrl`, `replyToFormCheck`, `deleteFormCheck`. NOTE: messaging + form checks have **no demo path** (real Supabase only) — both are guarded by `window._demoMode` and skipped in the `?demo=1` hero demo.

## AI assistant ("Ask AI")

A floating assistant on both dashboards (`ai-chat.js` + `ai-chat.css`, loaded after the page's own scripts). Role is picked from the URL: `coach.html` → coaching assistant, else athlete training assistant.

**Two runtime modes, resolved in `init()`:**
- **LIVE** — a signed-in user on a real page. `liveReply()` → `DB.aiChat()` → `sb.functions.invoke('ai-chat')` → the `ai-chat` edge function → Google Gemini. The Gemini key lives only in the function's secrets; the browser never holds it. Supabase auto-attaches the session JWT, which the function (Verify JWT ON) validates.
- **MOCK** — demo mode (`?demo=1`), signed-out, or any live failure. Answers are computed locally in `ai-chat.js` from real Store data (`snapshotFor`, `athleteAnswer`, `coachAnswer`, `weekRecap`, `draftProgram`, etc.). No network, no key, no cost. Also the graceful fallback: first live failure flips `liveDown = true` and the rest of the session uses mock.

`LIVE_BACKEND` (const at top of `ai-chat.js`) is the master switch — set `false` to force mock everywhere.

**Cost is bounded three ways:** (1) the edge function clamps `maxOutputTokens` (800), system length, and history; (2) a per-user daily cap (`ai_chat_usage`, `AI_DAILY_CAP` env, default 20); (3) **the budget you set on the Google Cloud project.** Gemini 2.0 Flash is ~$0.10/1M input + $0.40/1M output, so at the per-user cap each chat costs fractions of a cent.

**Provider note:** the assistant is provider-agnostic above the edge function. Swapping Gemini for OpenAI / Anthropic / Groq means rewriting only `ai-chat.ts` (endpoint + request/response shape + secret name) — the client, mock fallback, and `ai_chat_usage` cap are untouched.

**Dual-provider with fallback:** `ai-chat.ts` tries **Gemini first** (`gemini-2.5-flash`, cheapest) and **automatically falls back to Groq** (`llama-3.3-70b-versatile`) whenever Gemini errors (billing/quota/outage). Only if *both* fail does it return 502 and the client flips to local mock. So the assistant stays Live as long as either provider works. Set `GEMINI_API_KEY` and/or `GROQ_API_KEY` (at least one required) as function secrets — set both for the fallback to engage. `callGemini`/`callGroq` are the only provider-specific code; everything else is shared.

**To bring it live:** deploy `supabase/functions/ai-chat/index.ts` (Verify JWT ON — `./deploy.sh` handles it), set `GEMINI_API_KEY` (key from aistudio.google.com/apikey — billing must be enabled on the Google Cloud project) **and** `GROQ_API_KEY` (free, no card, from console.groq.com/keys) as secrets, run `sql/migration-ai-chat-usage.sql`. Until then it runs in mock mode for everyone (graceful). `review.html` is a **local-only** dev tool and must NOT ship to main.

## Known gaps / pending work

- **Self-serve data export** — DONE this session (Settings → Export, JSON + CSV; program→spreadsheet CSV on both dashboards). Copy in privacy.html + faq.html updated to describe it.
- **Self-serve account deletion / erasure (#33)** — DONE (PREP — enable = 1 migration + `./deploy.sh`, no new secret). Replaced the old `deleteMyAccount()` **facade** (a client `sb.from('profiles').delete()` that RLS silently blocked — never removed the auth user, never cascaded, and would have hit `program_sales` RESTRICT FKs anyway) with a real server-side erasure. Three pieces: (1) `sql/migration-account-deletion.sql` makes the cascade safe — `program_sales.buyer_id/coach_id/marketplace_program_id` become `DROP NOT NULL` + `ON DELETE SET NULL` (retain the financial/tax row, sever the identity — "keep the invoice, anonymize the party"), and a guarded DO block upgrades `profiles→auth.users` to `ON DELETE CASCADE` iff needed. (2) `supabase/functions/delete-account/index.ts` (Verify JWT ON, `WITH_JWT` in deploy.sh): **uid comes ONLY from the caller's JWT** (never the body → you can only erase yourself), rate-limited via the #26 helper (`delete-account`, 5/hr), sweeps the caller's Storage objects (`<uid>/` in `form-checks` + `avatars` — `deleteUser` does NOT touch Storage) then `auth.admin.deleteUser(uid)` cascades the rest; returns `{deleted:true,purged}` or a clean 500 `{error,detail}`. Uses only the auto-injected `SUPABASE_URL`+`SUPABASE_SERVICE_ROLE_KEY` (no new secret). (3) Client: `DB.deleteAccount()` (db.js, `sb.functions.invoke('delete-account',{body:{}})`, unwraps the FunctionsHttpError JSON like `sendInvite`) + a rewritten `deleteMyAccount()` (app.js) — a **type-`DELETE`-to-confirm** modal (`ensureDeleteAccountModal`/`onDelAcctInput`/`confirmDeleteMyAccount`) that lists what's erased vs kept-anonymized, offers **Export-first** (reuses `exportMyData`), and on success toasts → `signOut` → `index.html`. **Honest failure path:** a server error does NOT sign you out or pretend it worked — it surfaces the real reason + the manual `powalifta1320@gmail.com` fallback. `window._demoMode` short-circuits with a toast (no network). privacy.html "How long we keep data" + "Your rights" copy updated to describe the now-working self-serve deletion (immediate erasure, tax rows anonymized). Verified in Claude_Preview at 1280 + 375 (modal renders, gate toggles disabled→enabled on exact "DELETE", responsive with no overflow); app.js parses clean; no new pure helper so tests.html untouched. **To enable:** run `sql/migration-account-deletion.sql` (BEFORE deploy, else the cascade is blocked by `program_sales` and `deleteUser` fails cleanly → client shows the email fallback), then `./deploy.sh`.
- **In-app subscription management (#34)** — DONE (PREP — the buildable-now path works with **no secret**; the signed-deep-link enhancement is the ONE gated part). The upgrade modal (`openUpgradeModal`) already handled buying a NEW plan; this adds the missing half — letting an EXISTING subscriber see plan status and reach the Lemon Squeezy billing portal. **All new code in app.js** (after `openUpgradeModal`): `LS_BILLING_PORTAL` (the LS no-code email-magic-link portal — works for any customer, no secret); pure `subscriptionStatusInfo(status)` mapping LS's raw `subscription_status` → `{label,tone}` (active/on_trial→ok, paused/cancelled→warn, past_due/unpaid→bad, expired/inactive/unknown→null); `renderPlanManageCard(container)` (**coaches only** — collapses the box for athletes; shows tier in Anton + a tinted status badge + "Using N of M athlete slots" + a red payment-problem/cancellation nudge + **Change plan**→`openUpgradeModal` + **Manage billing**→`openBillingPortal`; free tier shows "See plans" only, no billing button); `openBillingPortal()` (opens the tab **synchronously** inside the click so the popup blocker can't eat it, then upgrades the URL to the signed deep link if `DB.subscriptionPortal()` returns one, else falls back to `LS_BILLING_PORTAL`). Wired a coaches-only `#setPlanBox` into the Settings modal (rendered in `openSettingsModal`, above the referral card). db.js: `DB.subscriptionPortal()` (invoke `ls-portal`, unwrap FunctionsHttpError JSON like `deleteAccount`; any `{ok:false}` → client falls back to the no-code portal). PREP edge fn `supabase/functions/ls-portal/index.ts` (Verify JWT ON, `WITH_JWT` in deploy.sh) resolves the caller's OWN `ls_subscription_id` server-side (JWT→own profile) and calls the LS API for the signed `customer_portal` URL — gated on the optional new secret `LEMON_SQUEEZY_API_KEY` (added to `.env.example`); degrades to HTTP 200 `{ok:false}` when unset. Demo-safe (`openBillingPortal` toasts "Preview only in demo mode"; demo coach is pro/active). Verified in Claude_Preview at 1280 + 375 across pro/active (green Active badge + both buttons), past_due (red "Payment problem" badge + danger nudge), free (See plans only), and athlete (box collapsed); `subscriptionStatusInfo` unit-checked across all 11 status inputs; app.js + db.js parse clean. **To fully enable the signed deep link:** set `LEMON_SQUEEZY_API_KEY` as a function secret + `./deploy.sh` (ls-portal is in WITH_JWT). Until then, "Manage billing" already works via the no-code portal. No new pure-helper test file touched (helper is trivial/switch); no DB migration (subscription columns already exist). Still no in-app plan-change without a round-trip to LS checkout (by design — LS is Merchant of Record).
- **Failed-payment / dunning UX (#35)** — DONE (buildable-now — no secret, no migration). A page-wide plan-lapse banner on the coach dashboard that reacts to `window._user.subscriptionStatus` (the raw LS status bootstrap maps onto the user). **All new code in app.js** (right after `renderPlanManageCard`): pure `dunningInfo()` (coaches on a paid tier only; maps status → `{key,severity,icon,title,body,cta}` — `past_due`/`unpaid`→**critical** ("Payment problem" / "Fix billing"), `cancelled`→**warn** ("set to cancel" / "Resume plan"), `paused`→**warn** ("paused" / "Resume plan"), everything else→null; body names the tier + the Free (3) floor it reverts to); `dismissDunning(key)` (per-session `sessionStorage['powa-dun-dismiss-'+key]`); `renderDunningBanner(container)` (critical = red, **non-dismissible**, `btn-primary`; warn = amber, **dismissible** ×, honours the session flag; CTA always → `openBillingPortal()` from #34). All `window`-exported. Mounted `<div id="dunningMount"></div>` at the top of coach.html's `<main class="dash-main">` and called `renderDunningBanner(...)` in the coach bootstrap after `renderRoster()`. Styles: `.dunning-banner`(`.critical`/`.warn`) + `.dun-icon`/`.dun-text`/`.dun-title`/`.dun-body`/`.dun-actions`/`.dun-dismiss` in styles.css (severity colours match the `.att-row` convention — red `var(--red)`, amber `#f5a623`; flex-wraps at ≤560px). Demo-safe: demo coach is `active` so nothing shows; reuses #34's `openBillingPortal` (toasts in demo). Verified in Claude_Preview at 1280 + 375 by eval-forcing `subscriptionStatus` to `past_due` (red, no dismiss, "Fix billing") and `cancelled` (amber, dismissible, "Resume plan"), then restored to `active` (banner gone); app.js parses clean. No pure-helper unit test (trivial switch); the LS webhook already writes `subscription_status` so no migration. Relies on `ls-webhook.ts` keeping `subscription_status` current (it does).
- **Email unsubscribe + preferences (#36)** — DONE. The public, **unauthenticated** one-click unsubscribe path every email footer links to (the in-app Settings → Email prefs UI + `email_preferences` table were already shipped; this closed the real gap). Three pieces: (1) `supabase/functions/unsubscribe/index.ts` — a Verify-JWT-**OFF** edge fn (added to `NO_JWT` in deploy.sh) that is a pure `token → whitelist-patch` primitive: validates the `unsub_token` UUID, resolves the row **server-side via service role** (RLS never opened to anon), and reads/updates ONLY the two booleans (`weekly_digest`, `product_emails`). `POST {token, set?}` → `200 {ok,weeklyDigest,productEmails}`; unknown/malformed token → friendly `200 {ok:false, reason:'invalid link'}` (never leaks whether a token exists). Rate-limited via the #26 shared meter (`bucket:'unsubscribe'`, keyed token+IP, 60/hr, fail-open). No new secret (`SUPABASE_URL`+`SERVICE_ROLE` auto-injected); no new migration (reuses `email_preferences`, incl. its `unsub_token uuid UNIQUE`). **Security model:** the token IS the capability — grants control over that user's two email booleans only, exposes no identity/other data. (2) `DB.unsubscribeEmail(token, set)` in db.js (right after `updateEmailPrefs`) — `sb.functions.invoke('unsubscribe',{body})`, unwraps the function's JSON reason from `error.context` on FunctionsHttpError, always returns `{ok, weeklyDigest?, productEmails?, reason?}`. (3) `unsubscribe.html` — standalone token-landing page (modeled on reset-password.html; `noindex,nofollow`; shared nav/fonts/styles; `bootstrap(null, cb, {staticPublic:true})`). UX: **honours the one-click intent** — on load it calls `unsubscribeEmail(token,{weeklyDigest:false})` so the click itself unsubscribes from the weekly digest, then renders a green confirmation banner + two `.email-pref-row` toggles (weekly / product) for granular control **and undo** (flip weekly back on). Each toggle saves optimistically via the same endpoint, trusts the server's returned state, reverts + flashes an error on failure. No token / invalid / server-unreachable → friendly err banner + "Manage preferences in Settings" → athlete.html. Page-specific `.unsub-*` styles (incl. `.unsub-result.ok/.err` with `.ur-ic` circle + `display:block` `.ur-title`/`.ur-body`). **Graceful degradation:** with the function undeployed it shows the friendly "This link isn't valid" fallback (verified). Verified in Claude_Preview at 1280 + 375 (happy-path banner stacks correctly, toggles on-brand: weekly off / product red-checked; err fallback clean). db.js + app.js parse clean. **To bring live:** `./deploy.sh` (deploys `unsubscribe` in the NO_JWT group). Until deployed, footer links land on the friendly fallback — nothing breaks.
- **2FA / account security (#37)** — DONE (buildable-now for the most part — **no secret, no edge function, no migration**; uses Supabase Auth's **native TOTP MFA** entirely through the anon client). Three surfaces. (1) `db.js` — 7 soft-failing `mfa*` wrappers after `updatePassword` (never throw; every path returns `{ok,reason?}`): `_mfaUnavailable(reason)` (classifies "not enabled"/"disabled"/"unsupported" errors), `mfaListFactors()` (`{ok,factors,verified}`), `mfaEnrollTotp(friendlyName)` (**prunes any leftover `unverified` factor first** so an abandoned enroll can't block a retry, then `sb.auth.mfa.enroll({factorType:'totp'})` → `{factorId,qr,secret,uri}`), `mfaVerifyEnroll(factorId,code)` (challenge→verify, elevates to AAL2), `mfaUnenroll(factorId)`, `mfaAssurance()` (`getAuthenticatorAssuranceLevel` → `{currentLevel,nextLevel}`), `mfaChallengeVerify(code)` (finds the verified factor, challenge→verify — used at login step-up). The `unsupported` flag lets the UI **hide the whole section** when the project has MFA disabled (graceful). (2) `app.js` — a **2FA card in the shared Settings modal** (`#setMfaBox`, rendered by `renderMfaCard` right after the notification prefs): Anton "2FA" + tinted status pill (green "On" / red "Off" via `_subStatusColor`) + Enable/Turn-off button. `openMfaEnrollModal()` builds `#mfaEnrollModal` (QR `<img>` in a white box + manual Space-Grotesk secret + 6-digit `#mfaCode` input, Enter-to-submit, `autocomplete="one-time-code"`); `submitMfaEnroll()` validates 6 digits → `DB.mfaVerifyEnroll` → toast + re-render; `disableMfa(factorId)` `confirm()`s → `DB.mfaUnenroll`. All `window`-exported; demo-safe (demo shows "Off" + Enable that toasts "Preview only in demo mode" — the real enroll/disable are `_demoMode`-gated). (3) `index.html` — **login-time AAL2 step-up** in `doLogin()`: after `DB.signIn`, `DB.mfaAssurance()`; if `nextLevel==='aal2' && currentLevel!=='aal2'` it calls `promptMfaChallenge()` (a `Promise<boolean>` modal `#mfaChallengeModal` — 6-digit input, Verify/Cancel, Enter submits) and **signs the user back out + aborts** if they cancel/fail, so an MFA account can't finish login unchallenged. **Security model:** MFA state lives in Supabase Auth (factors table), enforced by Supabase; the client only enrolls/challenges. The login gate is the pragmatic PREP protection; **full RLS-level aal2 enforcement** (policies keyed on the JWT's `aal` claim) is the documented "to fully enforce" future step. Verified in Claude_Preview at 1280 + 375: Settings 2FA card (Off/red pill + Enable), enroll modal (QR white box + mono secret + centered code input + red "Verify & turn on"), and the login challenge modal all render on-brand and fit both viewports. db.js + app.js parse clean; no pure helper → tests.html unchanged. **To bring live:** enable **TOTP MFA** in Supabase Dashboard → Authentication → Multi-Factor (the only gated step — no deploy/secret); optionally add aal2-keyed RLS policies to hard-enforce. Until enabled, `mfaListFactors` returns `unsupported` and the Settings section **self-hides** — nothing breaks.
- **Accessibility / contrast / responsive audit (#7)** — DONE. Three fixes, all additive. (1) **Contrast (WCAG AA):** `--text-4` (the faintest text tier, which drives `.faded` body text AND every `input/textarea::placeholder` incl. `.note-input`/`.coach-note-input`/`.set-note`/`.ex-note`) was the only failing token — bumped in both themes in styles.css: dark `#56565e`→`#808089` (bg 5.03:1 / bg-1 4.82 / bg-2 4.61, all clear 4.5), light `#a0a0aa`→`#6a6a72` (bg 5.01 / bg-1 5.36 / bg-2 4.72). Only on the rare deepest surface (bg-3) does it land ~4.3 (large-text tier). It's also used decoratively (severity dots, guide-crumb separators, dashed borders) — those just get marginally crisper (harmless). Every other token already passed AA; light `--red` on bg-2/bg-3 dips to ~4.1–4.45 (large-text-only) but is brand-locked, left as-is. (2) **Icon-button labels:** all **27** `<button class="modal-close">&times;</button>` X-buttons got `aria-label="Close"` (static HTML in index/admin/coach/athlete + JS template strings in app.js — the class string is identical across both forms so a per-file replace-all caught them). (3) **Skip links (WCAG 2.4.1 Bypass Blocks):** only index + athlete had one; added `<a class="skip-link" href="#…">Skip to content</a>` right before the shared `<nav>` on the **17** other public pages (about/admin/coach/coach-profile/exercises/faq/guide-dots/guide-e1rm/guide-peaking/guide-rpe/guides/leaderboard/marketplace/privacy/reset-password/terms/unsubscribe). Pages whose `<main>` already had an id reuse it (`#cpRoot`/`#exlibRoot`/`#lbRoot`/`#mkRoot`); the other 13 got `id="main"` added to `<main>`. Skipped 404 (no `<main>`), tests.html (dev harness), index-bold.html (experimental variant). The `.skip-link` CSS already existed (canonical rule at styles.css:1746 hides via `transform: translateY(-160%)` + `opacity:0`, reveals on `:focus`; an older dead duplicate at :1092 is overridden + harmless). **Recon confirmed non-issues:** all 22 pages have `lang` + viewport meta (no `user-scalable=no`); the 2 `<img>`s are alt'd; `font-display=swap` everywhere; all `*-fx.css` honour `prefers-reduced-motion`; the 840px hero-orb is clipped by `.hero{overflow:hidden}`. **Horizontal-scroll sweep:** 0 overflow at 375px on every page (`scrollWidth === innerWidth`). Verified in Claude_Preview at 1280 + 375 (index hero + faq); tests.html 143/143 green (aria-label additions to app.js modal strings didn't perturb any assertion).
- **Meta / SEO sweep every page (#8)** — DONE. Recon found most public pages already carried the full pattern (from #18's guide work); this closed the last 3 gaps. **Canonical pattern** (per public page): `<title>`, `meta name=description`, `link rel=canonical`, OG (`og:title`/`og:description`/`og:type`/`og:url`/`og:image` + `og:image:width=1200`/`height=630`), Twitter (`twitter:card=summary_large_image`/`title`/`description`/`image`); shared OG image `https://www.powalifta.com/og.png`. Gaps fixed: (1) **privacy.html + terms.html** were missing `og:description` + `og:image:width/height` + all 4 twitter tags — added, matching about.html. (2) **coach-profile.html** had every OG/Twitter tag EXCEPT `<link rel=canonical>` — added a static baseline (`…/coach-profile.html`) for non-JS crawlers, then a **per-coach JS refinement** at the `document.title` set-site in `renderCoachProfile()`: a `setMeta(sel,attr,val)` helper rewrites canonical + `og:url` to `…/coach-profile.html?id=<id>`, `og:title`/`twitter:title` to "`<name>` — Powerlifting coach on POWALIFTA", and description/`og:description`/`twitter:description` to the coach's bio (≤155ch, falls back to a generic line). Verified in preview: script parses (renderCoachProfile defined), no-id path keeps the static baseline, synthetic-coach simulation mutated all 7 tags correctly, 0 console errors. **Audit result:** all **14** indexable public pages (index, marketplace, guides, 4× guide-*, exercises, leaderboard, about, faq, privacy, terms, coach-profile) now carry the full set (coach-profile's grep shows `2` for canon/ogTitle/etc = the static tag + the JS selector string, not a duplicate element — confirmed one real tag each). **Excluded correctly:** 6 app/utility pages (athlete/coach/admin/reset-password/unsubscribe/404) carry `noindex, nofollow` → title-only meta is intentional. **robots.txt + sitemap.xml left as-is (already correct):** sitemap lists all 13 indexable pages, omits dynamic coach-profile (bare URL errors), the `index-bold.html` variant, and noindex pages; robots.txt disallows the 4 auth app pages + points at the sitemap. **SEO nuance recorded:** tests.html/index-bold.html/unsubscribe.html/404.html use `noindex` meta and are deliberately NOT robots-`Disallow`ed — a Disallow would stop crawlers fetching the page and seeing the noindex, defeating de-indexing. No JS/DB/test changes beyond the coach-profile head + render-fn edit.
- **Cookie consent banner** — DONE. Honest, privacy-first `cookie-consent.js` + `cookie-consent.css` drop-in (loaded after `styles.css` / `error-log.js` on every public page). The site sets no ad cookies; "Essential only" actually disables the cookieless Plausible via its `plausible_ignore` flag, "Accept" leaves it on. Choice persists in `localStorage['powa-consent']`, re-applied early on every load; banner only shows until a choice is made. privacy.html "Cookies and analytics" copy updated to match.
- **Bodyweight cut/bulk tracking** — DONE. Athlete Bodyweight tab now reads the weigh-in series, not just the latest number. New pure helpers in `app.js`: `linregSlope(xs, ys)` (least-squares), `bwCutBulkStats(series, goalKg, direction)` (7-day trailing moving average → smoothed "trend"; weekly rate via least-squares over the last 28 days; start→goal progress; ETA-to-goal when trending the right way; signed %BW/week), and `bwPace(slopeKgWk, curKg, direction)` (powerlifting pace read — cut sweet-spot ≈0.5–1.0 %BW/wk, lean bulk ≈0.2–0.5 kg/wk). `goalProgressPct` gained an optional 4th `start` arg (true start→goal fraction; the three existing 3-arg callers are untouched). `renderBodyweight()` (athlete.html) shows a pace chip + weekly rate + ETA in the goal card and overlays a slate 7-day-trend line (uses a new per-series `noArea` flag on `drawLineChart` so only the daily line keeps its area fill) with a Daily/Trend/Goal legend. No DB migration — `bodyweight` + `goals` (incl. `bw_direction`) already had the columns. Weight-class targets still not modelled (raw kg only).
- **Marketplace sort + filter** — DONE. `marketplace.html` has tier (category) filter chips (All / Short cycle / Full block / Premium), a sort dropdown (Featured / Most popular / Top rated / Price ↑ / Price ↓), and debounced search. Server side: `DB.listPublishedPrograms({tier, search})` filters by tier + `ilike` over title OR description, ordered by `sold_count`; sort is applied client-side in `sortPrograms()` after review stats aggregate (so "Top rated" reuses the same stats the cards show).
- **Meet results — competition history + best total** — DONE. The forward-looking meet-day attempt planner already lived inline in athlete.html (openers/seconds/thirds from e1RMs, warm-up ramp, countdown — recomputed from logged sets, no storage). This adds the OUTCOME half: a `meets` table (`migration-meets.sql`) storing the best SUCCESSFUL attempt per discipline (`squat_kg`/`bench_kg`/`deadlift_kg`, **null = bombed/not contested**), plus name/date/federation/weight_class/bodyweight/notes. RLS = own-rows-only (cloned from push-subscriptions) + coach READ-ONLY via the live-link join. db.js: `mapDbMeet`/`mapJsMeet` (nullable numerics preserve `null` — never coerce a bombed lift to 0; upsert id only when present), `listMeetsForAthlete`, `listMeetsForAthletes`, `saveMeet` (upsert → `.select('*')` returns the row so the client picks up the generated id), `deleteMeet`; all wired into `hydrateAll` (athlete + coach paths). Pure helpers in app.js (global, classic-script): `meetTotal` (null lifts → 0), `meetIsFullTotal` (all three present), `bestMeet` (highest FULL total for the athlete, bombed meets excluded), `meetDots` (DOTS from real total + BW). UI in athlete.html Progress tab: `renderMeetHistory()` (red-accented best-total badge + result cards with S/B/D/Total grid, is-best ring on the top meet, notes, Edit/Delete) + a rebuilt-on-open modal (`ensureMeetResultModal`/`openMeetResultModal`/`saveMeetResult`/`deleteMeetResult`) whose unit labels track the live kg/lb pref. Demo-safe (2 demo meets; saves/deletes short-circuit DB in `_demoMode`). CSS `.meet-*`/`.mc-*` in styles.css (+ new `.btn-xs`); 7 unit tests in tests.html (97 total pass). Verified in preview at 1280px + 375px (CRUD round-trip, responsive lifts grid). No model for all-9-attempts or weigh-in-day water cut — outcome only.
- **Marketplace training-focus taxonomy** — DONE. A real `category` axis ON TOP of `tier` (which is length/format). `marketplace_programs.category` (`migration-marketplace-taxonomy.sql`, nullable text + index, no CHECK). Client-side source of truth `MARKET_CATEGORIES` + `marketCategoryLabel()` in app.js (keys: strength/hypertrophy/peaking/beginner/bench/weakpoint → labels), exported on `window`. coach.html publish modal: the old "Category" select is relabeled **Length / format** (`pubTier`) and a new **Training focus** select (`pubCategory`, populated from the taxonomy on open) sits beside it; `confirmPublish()` reads + passes `category`; coach listing meta shows the label. marketplace.html: a second **Focus** filter-chip row (built from `MARKET_CATEGORIES`) filters **client-side** by `p.category` (never pushed into the query, so it's pre-migration-safe); a muted `.mk-cat-tag` pill renders on browse cards + the detail page next to the tier badge. db.js: `mapDbMarketplaceProgram` adds `category`; `mapJsMarketplaceProgram` includes it **only when truthy**; `submitMarketplaceProgram` + `updateMarketplaceProgram` **retry without `category`** on a column-missing error — so publishing/editing works before the migration runs. Demo-safe (publish still guarded by `_demoMode`). Null-category (legacy) listings show no pill and only match "All".
- **Referral / affiliate program** — DONE. Every profile gets a 6-char `referral_code` (unambiguous alphabet, auto-generated on insert + backfilled by `migration-referrals.sql`). A `?ref=CODE` link is captured to `localStorage['powa-ref']` at the top of `bootstrap()` (`captureReferralFromUrl`) so it survives the whole signup round-trip (incl. email confirmation), then stamped onto the new user's OWN `profiles.referred_by_code` once authenticated (`applyPendingReferral` → `DB.applyReferral`, mirroring the invite-claim pattern; idempotent, fire-and-forget). A SECURITY DEFINER trigger resolves code→referrer and writes one `referrals` row (self-referral-proof, unique per referred user; both code columns are write-once and silently pinned so a normal settings save can't rewrite attribution). The Settings modal shows a shared `renderReferralCard(container)` (link + Copy button + "N lifters joined through you" list via `DB.listMyReferrals`); demo users carry codes `DEMO42`/`COACH7` so it renders in the `?demo=1` showcase. db.js: `mapDbReferral`, `DB.listMyReferrals`, `DB.applyReferral`; profile mappers + `window._user` gained `referralCode`/`referredByCode`.
- **Referral rewards / ledger** — DONE (on top of attribution). A persistent claim ledger sits above the existing referral count. `referral_reward_claims` table (`migration-referral-rewards.sql`): `(user_id, tier_at)` unique, `status` `requested|fulfilled|declined`, select-own RLS, **no client INSERT** — the only writer is a SECURITY DEFINER rpc `claim_referral_reward(p_tier_at int)` that counts the caller's OWN `referrals` and RAISEs `check_violation` on any unearned tier (same earn-gate trust pattern as `claim_invite`/`accept_coach_request`; idempotent via `ON CONFLICT DO NOTHING`). Reward COPY is client-side in `REFERRAL_TIERS[].reward` (tunable without a migration; tiers 1/3/5/10/25 → badge, Basic-comp, Pro-comp, Premium perks); fulfilment is **deliberately manual** (admin flips `status='fulfilled'` via service role — no payment backend touched, honest with POWALIFTA's "recognition, not a cash promise" stance). Pure helper `referralRewardsLedger(count, claimed)` (app.js) merges tier defs + real count + claim rows → per-tier `earned/claimed/status/remaining`; `referralRewardsHtml()` renders the ledger (earned+unclaimed → Claim button; claimed → Requested/Fulfilled pill; locked → "N more"). `renderReferralCard` fetches `DB.listMyReferrals` + `DB.listMyRewardClaims` in parallel and appends the ledger; `claimReferralReward(tier, btn)` calls `DB.claimReferralReward` then re-renders. db.js: `mapDbRewardClaim`, `DB.listMyRewardClaims`, `DB.claimReferralReward` (via `sb.rpc('claim_referral_reward',{p_tier_at})`). Demo-safe: demo count=2 → only tier 1 claimable; Claim flips `window._demoRewardClaims` locally (no network) so the `?demo=1` showcase demos both Claim → Requested states. `.ref-reward` CSS in styles.css. 8 unit tests for the pure ledger (tests.html, 105/105 green). Verified at 1280 + 375 (Settings → Refer a lifter).
- **Messaging realtime** — DONE. `messages` is now on the `supabase_realtime` publication (`migration-messages-realtime.sql`). `DB.subscribeMessages(filter, onInsert)` (db.js) opens a `postgres_changes` INSERT channel (server-side filter + `mapDbMessage`) and returns a teardown fn. Open threads append live (`renderMessageThread` subscribes inside the non-demo branch, tearing down the prior channel on each re-render via `container._msgCleanup`); coach roster badges debounce-refresh off a `coach_id=eq.<uid>` channel (`subscribeRosterMessages` → `scheduleUnreadRefresh`); athlete Coach tab lights a `.tab-dot` off an `athlete_id=eq.<uid>` channel (`subscribeCoachMessages`). RLS still gates delivery (subscriber only gets rows `msg_member_read` would let them SELECT). Demo-safe: every subscribe early-returns on `window._demoMode`; modal close + tab re-render tear channels down (no leaks). Sender's own echo is a client-side no-op (id dedupe).
- **Messaging read-receipts** — DONE. Live "Sent"/"Seen" status under my most-recent message (iMessage-style — the receipt rides only the last `mine` bubble). `DB.subscribeMessageReads(filter, onUpdate)` (db.js, mirrors `subscribeMessages` but `event:'UPDATE'`) streams `read_at` stamps — possible because `migration-messages-realtime.sql` already set `REPLICA IDENTITY FULL` on `messages`, so UPDATE payloads carry the full row (**no new migration**). `renderMessageThread` (app.js) threads `id`+`readAt` through `buildTimeline`, paints a `.msg-status` marker (`Seen · <time>` when `readAt` set → red `.is-seen`; else `Sent` → muted) under the last mine message, and opens a parallel UPDATE channel combined with the INSERT channel into a single `container._msgCleanup` teardown. Demo-safe (no realtime in demo; mine-bubbles render from each message's own `readAt`, so the showcase shows a "Seen" on the athlete Coach tab). CSS `.msg-status`/`.msg-status.is-seen` in styles.css. Verified at 1280 + 375 (last mine shows red "SEEN · …"; nulling `readAt` falls back to muted "Sent").
- **Coach roster "needs attention" triage** — DONE. New Roster-tab panel (`buildAttentionPanel` in coach.html) surfaces who to look at first: inactive (`never`/`stale` flag), **stalling** (e1RM flat/down), low adherence (logged ÷ programmed <40%), plus a 🏆 wins ribbon for athletes who PR'd this week. Backed by two new pure helpers in `app.js`: `getE1RMMomentum(athleteId, windowDays=21)` (best `e1rmComp` in the last window vs the prior window per main lift → `stalling` when paired data exists and nothing beat tolerance — needed because `bestE1RM` is all-time and can't see a plateau) and `getRosterAttention(coachId)` (joins momentum + `getRosterOverview` adherence/recency + weekly PR count into a severity-ranked list). Deep per-athlete analytics already lived on the Progress tab (`renderRosterAnalytics`); this is the at-a-glance triage. Demo roster renders it. Styles: `.attention-panel`/`.att-*`/`.tab-dot` in styles.css.
- **Weight-class targets** — DONE (make-weight readout). `weightClassInfo(bwKg, sex)` in `app.js` (atop the existing `IPF_CLASSES`/`weightClassFor`) returns current class, kg under the ceiling, and kg to cut to drop a class. Surfaced as a `.wc-line` in the athlete strength-score card (`buildStrengthScoreCard`, athlete.html): "X under the 83kg ceiling · cut Y to make 74kg" (or "Right on the limit" / "Top class"). DOTS + class label + strength tiers already rendered there. Raw IPF/USAPL kg classes only — no weigh-in-day water-cut modelling or meet-date countdown.
- **Admin platform analytics (growth + revenue)** — DONE. New card at the top of the admin Overview (`renderGrowthRevenue(area)` in admin.html, called first inside `renderOverview()`): six `.stat-tile`s (Est. MRR [featured] + ARR sub, Paying coaches + conversion %, GMV all-time + sale count, Platform take, New signups 30d, GMV 30d), a `.mrr-chips` row (paying-coach count per tier), and a 12-week split `drawLineChart` (athletes vs coaches new-signups, red area + slate line). Backed by one pure helper `computePlatformAnalytics(profiles, sales, opts)` in app.js (global; `opts.now`/`tierPrices`/`weeks` injectable for tests). **Dual-case-tolerant by design** — admin `DB.listAllProfiles()` returns RAW snake_case rows while `DB.listAllSalesAdmin()` returns mapped camelCase, so the helper reads both (`user_type`|`userType`, `payoutStatus`|`payout_status`, etc.). MRR counts billing-live subs (`active`/`on_trial`/`trialing`/`past_due`, **plus** a paid tier with absent status → covers admin manual `planSelect` tier sets); `free`/`cancelled`/`paused`/`expired` excluded. `TIER_PRICES={basic:25,pro:50,premium:100}`. GMV/take sum non-`cancelled` sales (`cancelled`=refund). No DB call (reuses the `cache.profiles`+`cache.sales` already loaded), no migration — display-only. Reused primitives: `drawLineChart`, `.stat-tile`/`.chart-card`, `fmtMoneyCents`, `ICONS`, `TIER_LABELS`; only new CSS is inline `.mrr-chips`/`.mrr-chip` in admin.html. 6 unit tests in tests.html (143 total green). Verified at 1280 + 375 (tiles collapse to 1 col, chips wrap, chart re-bakes at mobile width, no overflow) via a temp harness that eval'd the REAL extracted function against a synthetic cache (harness deleted, not shipped — same rule as review.html). Also fixed 2 pre-existing side-stripe `border-left` accents in admin.html (marketplace-preview desc block + `.admin-banner`) to full-border/tint per the design guardrail.
- **Production error monitoring (admin visibility + client flood guards)** — DONE (PREP — enable steps are secret/deploy-gated). Two halves. **(1) Admin can now SEE forwarded errors.** New "Client errors" sidebar tab in admin.html (button `#tab-errors` + `ICONS.note` label, click handler → `loadClientErrors()`, own `<section data-pane="errors">` with a `.admin-search`). `loadClientErrors()` lazy-fetches `DB.listClientErrors(500)` into `cache.errors` **only on first open** (`cache.errors` is intentionally left `undefined` in the cache-init + post-`Promise.all` blocks so the tab is the sole loader — no cost on every admin login). `renderClientErrors()` groups via `groupClientErrors(list)` (Map keyed `kind|msg|src|line`, sorted by newest `last` desc, tracks occurrence `count` + freshest sample), renders a `.tbl-card` table (#/Kind/Error/Source/Last seen; `errRelTime(ms)` relative stamps; kind badge PROMISE=gold / JS=red; expandable `<details>` with URL/File/UA/full stack) with a documented **empty-state** (setup steps) when there are no rows; search filters by msg/src/url. **Every error-derived string is `escHtml`'d** — the rows are untrusted browser input the edge fn stored verbatim (verified: an `<img src=x onerror=alert(1)>` payload renders as inert text, no DOM injection). db.js: `mapDbClientError(r)` (snake→camel, `received_at`→`receivedAt` ms, `ts`→ms) + ADMIN-block `listClientErrors(limit=500)` (`.order('received_at',desc)`, catch→`[]` so a missing table/policy degrades gracefully). New `sql/migration-client-errors-admin-read.sql` adds a single admin-only SELECT policy (`is_admin()`) on `client_errors` — still NO browser insert/update/delete (writes stay service-role `send-client-error` only). **(2) Client forward is now flood-proof.** error-log.js `maybeForward(entry)` replaces the raw POST: hard per-session cap (`FORWARD_MAX_PER_SESSION=40`), per-signature dedupe (`signatureOf` = `kind|msg|src|line`, each unique bug forwarded once/session), `navigator.onLine===false` skip, optional `FORWARD_SAMPLE_RATE`; local capture (localStorage + console) stays **unthrottled**. `window.__powaErrorStats()` exposes `{forwarded, uniqueSignatures, cap}`. Deliberately did NOT add uid/build to the payload — `send-client-error.ts` inserts a FIXED field set and drops unknowns, and the rows are meant to stay PII-light. No live-DB edit. **To enable (secret/deploy-gated):** set `SUPABASE_ERROR_ENDPOINT` in error-log.js to the deployed `send-client-error` URL (+ optional `SENTRY_DSN`), `./deploy.sh` the function, run `migration-client-errors.sql` then `migration-client-errors-admin-read.sql`. Until then: local capture works, forward is a no-op, and the Errors tab shows its enable-steps empty state. 143 unit tests still green (no new pure helper — render/group live in admin.html inline). Verified at 1280 + 375 via a temp harness that eval'd the REAL extracted admin.html renderers against a synthetic 7-error/4-signature cache (grouped rows [3,1,2,1], PROMISE/JS badges, "7 occurrences · 4 unique" summary, XSS-escaped, empty state; harness deleted, not shipped — same rule as review.html). Mobile note: the errors table shares `.tbl-card` (`overflow:hidden`) with all 8 admin tables, so it squeezes identically on 375 — a pre-existing global admin-table trait for the responsive audit (#7), not new here; no page-level overflow.
- **Edge-function rate limiting / abuse guards** — DONE (PREP — enable step is one migration, no new secrets). A reusable, deploy-safe throttle layer over the edge functions. **One migration** `migration-edge-rate-limits.sql`: an `edge_rate_limits` table (`(bucket, identity, window_start)` PK, RLS enabled with **NO policies** → service-role only) + an atomic **`rl_bump(bucket, identity, window_secs, limit)`** SECURITY DEFINER RPC that does a fixed-window counter via a single race-free upsert (`insert … on conflict do update set count = count + 1 returning count`), epoch-aligned window (`to_timestamp(floor(epoch/window)*window)`), returning `{allowed, cur, limit_val, reset_at}`; plus an optional `rl_gc()` sweep (drops windows >24h). **One shared helper** `supabase/functions/_shared/rate-limit.ts` (the leading-underscore dir is bundled but never deployed as its own function — the standard shared-code slot): `clientIp(req)` (first x-forwarded-for hop → x-real-ip → 'unknown'), `rateLimit(admin, {bucket,identity,limit,windowSecs})` → `RateVerdict`, and `rateLimitResponse(verdict, extraHeaders)` (429 + `Retry-After` + `{error:'rate_limited',…}`). **Fail-OPEN everywhere** — no admin client, no identity, missing RPC, or any error all return `ok:true, degraded:true`, so wiring the function code is safe to ship BEFORE the migration runs (the guard is simply inert until `rl_bump` exists). **5 functions wired**, reusing each one's existing service-role client + caller id (no new secret, no new client): `send-client-error` (IP-keyed, bucket `client-error`, 60/60s — the real DB-flood surface), `send-welcome` (two limits: IP `welcome-ip` 20/hr **and** recipient-email `welcome-email` 3/day, since it's unauthenticated and email-bombable), `send-invite` (uid `invite` 40/hr), `send-program-assigned` (uid `program-assigned` 60/hr), `send-push` (uid `push` 180/hr — chattier, so higher). The three JWT-ON senders were already gated on a live coach↔athlete link; this just adds a per-uid ceiling so a looping/compromised session can't fan out branded mail or notifications. The two authed-sender 429s pass `{ 'Access-Control-Allow-Origin': '*' }` so the cross-origin `sb.functions.invoke` caller can read them. **Deliberately NOT rate-limited:** both LS webhooks (HMAC-gated) and `send-weekly-digest` (CRON_SECRET-gated) — already fail-closed on a shared secret, no per-caller key to meter. **To enable:** run `sql/migration-edge-rate-limits.sql` (idempotent, order-independent) + redeploy the 5 functions via `./deploy.sh`; no new env/secret. Until then everything runs unthrottled exactly as before (fail-open). Edge-only change — no browser pure helper (tests.html unchanged, 137 green) and no UI (no preview needed).
- **DOTS leaderboard (opt-in public board)** — DONE. Bodyweight-adjusted, opt-in, public-read ranking — same denormalized pattern as `program_reviews`: a `leaderboard_entries` table (`migration-leaderboard.sql`) whose **row existence IS the opt-in**, carrying ONLY publishable fields (`display_name`/`sex`/`bodyweight_kg`/`total_kg`/`dots`/`basis`/`country_code`) so anon reads never touch `profiles` (no email/coach_id/subscription exposure, no boolean flag to sync). RLS: `leaderboard_public_read` (anon+auth SELECT true), owner-only insert/update/delete (`user_id = auth.uid()`); `dots DESC` index. db.js: `mapDbLeaderboardEntry`/`mapJsLeaderboardEntry` (camel↔snake, nullable numerics preserve `null`, sex/bw/country only when present), `listLeaderboard({sex,limit})` (`.order('dots',desc)`, optional `.eq('sex',…)` — text col so `.eq` is safe), `myLeaderboardEntry(userId)` (`.in('user_id',[id])` — UUID col), RLS-safe `upsertLeaderboardEntry` (UPDATE-then-INSERT), `removeLeaderboardEntry`. Pure helpers in app.js (global): `rankLeaderboard(entries)` (drops non-positive DOTS, sorts DOTS desc + total tiebreak, **standard competition ranks** — ties share, next skips; never mutates input), `leaderboardRowsHtml(ranked,opts)` (rank/name+sex+MEET+YOU/class+flag/total/DOTS columns, medal classes rank≤3, `lb-me` highlight), `demoLeaderboard()` (8 seeded lifters incl. `DEMO_ME`), async `renderLeaderboard(container,opts)` (demo → seed, else `DB.listLeaderboard` catch→[]), `computeMyLeaderboardEntry()` (from `window._user` + `latestBodyweight`/`currentTotal`/`getSexPref`/`dotsScore`; null unless bw + positive total + positive DOTS), async `refreshLeaderboardButton`/`toggleLeaderboard` (publish↔leave). New **`leaderboard.html`** public page (nav + hero + sex segmented filter [All/Men/Women] + live count + board + signed-in/out CTA card + footer; bootstrap `staticPublic:true`). Athlete opt-in control lives in `buildStrengthScoreCard` (athlete.html) — "Publish to leaderboard" button + privacy note + link, only past the bw+total early-return. Demo-safe (`window._demoMode` → `demoLeaderboard()` + `window._demoOnBoard` toggle, no network). `.lb-*` CSS in styles.css (grid rows, medals, MEET/YOU badges, sex tints, mobile class-column collapse <560px). 7 unit tests in tests.html (112 total green). Verified at 1280 + 375 (board ranks/medals/flags/YOU highlight, no overflow; opt-in publish↔leave round-trip). Manual/coach-free — `basis:'gym'` (comp-equivalent e1RM sum); meet-verified basis wired but not yet surfaced as a publish path.
- **Weekly volume landmarks (MEV/MAV/MRV)** — DONE. Athlete Progress tab now shows working-set counts this week per movement pattern against evidence-informed RP-style ranges (MEV minimum-effective → MAV → MRV max-recoverable), framed as a fatigue-management guide, not a target. Buckets key directly off the clean `log.lift` enum (`squat`/`bench`/`deadlift`/`push`/`pull`/`legs`) — no fuzzy name-matching. Pure helpers in app.js (global, after `liftBalance`): `VOLUME_LANDMARKS` (per-pattern mev/mav/mrv), `VOLUME_BUCKET_ORDER`, `VOLUME_ZONE_LABEL`, `volumeBucketFor(log)` (enum→bucket or null), `volumeZone(sets, lm)` (none/under/productive/high/over), `weeklyVolumeLandmarks(logs, athleteId, from, to)` (counts sets + hard sets [RPE≥7] in the [from,to) window, tags zone + `pctOfMrv`). UI `renderVolumeLandmarks(area)` in athlete.html renders inside `renderProgress()` after Lift balance: a "Weekly volume" card with one bar per trained pattern — set count, zone-tinted fill (under→blue, productive→green, high→gold, over→red/`--grad-red`), MEV/MAV/MRV tick marks on the track, zone label + range caption. Hidden entirely when nothing's logged this week; untrained patterns skipped. **GOTCHA fixed:** `mondayOf()` (athlete.html) returns a `'YYYY-MM-DD'` *string*, not a Date — use `Date.parse(mondayOf(new Date()))` for the week-start ms (a `.getTime()` call there throws and silently kills the rest of `renderProgress` below it). `.vol-*` CSS in styles.css (uses existing `--gold`/`--green`/`--blue`/`--red`/`--grad-red` tokens; `.vol-range` hidden <560px). 3 unit tests in tests.html (115 total green). Verified at 1280 + 375 (per-pattern bars, ticks, zone tints, no overflow; demo seeds current-week deadlift+pull logs so the section renders under `?demo=1`).

- **Exercise library w/ form cues** — DONE. A coaching-cue reference for the three competition lifts, their training variants, and the core accessories — keyed off the **same lift/variant taxonomy as logging** (`VARIANTS`/`ACCESSORY_EXERCISES`) so the log flow can pull contextual cues. Pure data + lookups in app.js (global, after `exercisesFor`): `EXERCISE_GROUP_LABELS`, `EXERCISE_LIBRARY` (23 entries — squat ×4, bench ×5, deadlift ×4, push/pull/legs ×3-4; each `{key,group,lift,variant?,multiplier?,name,pattern,summary,setup?,cues,faults?}`, mains carry full setup/cues/faults, variants carry summary+cues+multiplier, accessories carry summary+cues), and helpers `exerciseLibrary()`, `exerciseGroups()` (6 groups in lift-then-accessory order), `exerciseGroupLabel(g)`, `findExercise(key)`, `cuesForLift(lift,variant)` (exact lift+variant → base/Competition → first-of-lift; case-insensitive variant; null for unknown lift), `searchExercises(q)` (free-text over name/pattern/summary/variant/setup/cues/faults; empty/null → all), `exerciseCueBodyHtml(e)` (pure trusted-content HTML: summary + Set up/Execution/Fix-the-common-faults blocks; `''` for null). All content authored/trusted — safe to template. New **`exercises.html`** public page (nav + hero + movement-group filter chips + debounced search + live count + responsive card grid + disclaimer + footer; `bootstrap` `staticPublic:true`; multiplier badge per card — `1.0× · baseline` / `N× carryover` / none for accessories). Contextual cues also surface **in the athlete Today session**: `renderToday()` (athlete.html) appends a collapsible **Form cues** panel (mirrors the warmup-ramp shell) under each main-lift card via `exerciseCueBodyHtml(cuesForLift(ex.lift, ex.variant))` — main lifts only (lift+variant identifies the entry exactly; accessories match too loosely), additive, touches no save logic. `.exlib-*` card/body CSS is inline in exercises.html; the Today-session collapsible reuses `.cue`/`.cue-toggle`/`.cue-panel` + a `.cue-panel .exlib-*` restyle in styles.css so the shared `exerciseCueBodyHtml` output reads identically both places (red Execution markers, Space Grotesk block titles, faults arrows). **Gotcha:** exercises.html's content `.container` must use `padding-top`/`padding-bottom` longhand (NOT the `padding: 8px 0 64px` shorthand, which zeroes the side gutters and makes cards go edge-to-edge at mobile). Footer cross-links added on index.html + marketplace.html. 14 unit tests in tests.html (129 total green). Verified at 1280 + 375 (public page: chips filter, search narrows + clears, badges, no overflow; demo dashboard: 2 Form-cues panels on the 2 main lifts, expand/collapse, styled body, 0 errors). Cardio/other lifts get no cue panel. No DB work (pure client data).

- **Bulk / cohort template assignment** — DONE. The coach's "Apply template" flow now assigns one template to **many athletes at once** instead of one per open. `applyTplModal` (coach.html) swapped its single `<select id="applyTplAthlete">` for a **cohort checklist**: a "Select all" header + live "N selected" count over a scrollable `.cohort-list` of one `.cohort-row` per roster athlete (checkbox + name + a `.cohort-sub` note — red "Replaces current program" when they already have one, muted "No program yet" otherwise). `openApplyTemplate(tpl)` builds the rows + stamps the template name into the copy; `updateApplyTplCount()` keeps the count, the primary-button label ("Apply template" for ≤1 → "Apply to N athletes"), the row `.is-on` highlight, and the select-all tri-state in sync; `toggleApplyTplAll()` bulk-toggles. The per-athlete program construction was extracted into a shared **`buildProgramFromTemplate(tpl, athleteId)`** (reuses the athlete's existing program **id** so the DB upsert `onConflict=athlete_id` keeps the same row); `confirmApplyTemplate()` maps the checked ids → programs, commits them to the Store in **one** `Store.update` (replace-by-athleteId, matching the old single behaviour), then upserts each sequentially (skipped under `_demoMode`), counting failures for a partial-success toast, and finally jumps to the builder focused on the first selected athlete. Empty roster → a `.cohort-empty` hint + disabled select-all; 0 checked → "Pick at least one athlete" toast. `.cohort-*` CSS in styles.css (red accent-color checkboxes, `.is-on` red border + tint via `color-mix`, `max-height:260px` scroll). Coach.html-only, demo-safe, additive; no app.js helper (reads Store) so tests.html unchanged (137 green). Verified at 1280 + 375 (rows, select-all/count/label sync, checked-row red styling, bulk apply replaces all 4 demo programs with stable ids + builder jump, no overflow, 0 errors).
- **PR / achievement share cards** — DONE (extended the pre-existing card system). The branded 1080×1350 canvas generator (`generateShareCard(opts)`) + Web-Share/​download wrapper (`shareCardFromCanvas`) in app.js already backed three athlete cards — `shareMeetCard()` (meet-day plan), `shareWeekCard()` ("Share my week" recap), `sharePRCard(lift)` (per-lift PR, wired from both the PR-hit overlay and the Lifetime-PRs "Card" buttons). This session **added a strength-score / DOTS card**: `shareStrengthCard()` (athlete.html, after `window.sharePRCard`) reads `getSexPref`/`latestBodyweight`/`currentTotal`/`dotsScore`/`weightClassFor`/`bestE1RM`, early-returns with a toast unless a weigh-in **and** a positive total exist, then builds a `generateShareCard` with tag `STRENGTH SCORE`, big = DOTS (`bigUnit:'DOTS'`), sub = "Men's/Women's ⟨class⟩ class", and detail lines = Est. total + best squat/bench/deadlift e1RM (competition-equivalent, so it matches the Progress "This week vs last" / Lift-balance numbers, NOT the raw best-set tiles). A "📸 Make a card" `btn btn-sm btn-ghost` (matches the sibling "📸 Share my week" emoji convention — there is **no `ICONS.share` key**, don't reference one) sits in a new `.score-share` row inside `buildStrengthScoreCard`, immediately above the leaderboard opt-in; both are "put your number out there" actions, so `.score-share + .lb-optin` collapses the second divider (one shared group). Athlete-only, demo-safe (demo lifter has bw + total, so the card is real), additive; no app.js pure helper added → tests.html unchanged (137 green). Verified at 1280 + 375 (button renders with icon + label, generates a polished on-brand card — 400.4 DOTS / MEN'S 93KG CLASS / 593 kg total / all three lifts / powalifta.com footer — 0 console errors). `.score-share` CSS in styles.css.
- **Coach earnings dashboard** — DONE (enriched the pre-existing Marketplace earnings strip). `DB.listMySales(coachId)` + the basic tiles/recent-sales already existed; this adds a real rollup, a per-program breakdown, a this-month figure, and refund surfacing. Pure global helpers in app.js (after `MARKET_CATEGORIES`, testable, no DB): `coachEarningsSummary(sales, now?)` (single pass over `mapDbSale` rows → `{salesCount,totalEarned,thisMonth,pending,paid,refundCount,refundedCents,perProgram{pid:{count,earned}}}`; **`payoutStatus==='cancelled'` rows are excluded from earnings and counted as refunds instead**; `thisMonth` keys off `createdAt` ≥ local month-start; sums use `coachPayoutCents` = 80% of price) and `fmtMoneyCents(c)` (`$` + grouped 2-decimal, null-safe). `renderCoachMarketplace()` (coach.html) renders a 4-tile `.stat-grid` (**Total earned** [featured, with sale count] / **This month** / **Pending payout** / **Lifetime paid**), a `.faint` refund note when `refundCount>0` ("N refunds · −$X reversed"), a green per-program earned line on each listing card (`earn.perProgram[p.id]`), and refund-aware Recent-sales rows (newest-first; `↩ Refunded` struck-through in `--text-3`, else `✓ Paid out` / `⏳ Pending payout` in `--green`). Demo seed (`_buildCoachDemoData`): 3 listings + 14 sales (paid/pending/one cancelled) with **precise-instant `createdAt`** (`new Date(Date.now()-n*86400000).toISOString()`, not date-at-noon — a TZ month-boundary bug otherwise zeroed "This month") sorted desc to match live `listMySales` order. **App-wide latent bug fixed:** the `_countUpEl` stat count-up animation (app.js ~4853) was stripping non-numeric chars and forcing `.toFixed(1)`, which mangled EVERY money `.stat-value` (rendered "$343.87" as a stuck "48.1"). Rewrote it to capture a non-numeric prefix (`$`)/suffix + the source's own decimal precision + grouping, and re-apply them each frame — so money, plain ints, and decimals all count up in their original format. 8 unit tests in tests.html (137 total green). Verified at 1280 + 375 (tiles money-formatted & 2-col on mobile, per-program lines, refund note, recent-sales refund row, no overflow, 0 errors). No DB/migration work (helper is pure; `listMySales`/`mapDbSale` already shipped).
- **Content / SEO guide pages** — DONE. A theory-content system for organic search + internal linking, no DB. New **`guides.html`** hub (`.guide-hub` / `.guide-hub-card` grid) + four long-form articles: **`guide-rpe.html`** (RPE & RIR), **`guide-e1rm.html`** (e1RM formula), **`guide-dots.html`** (DOTS score), **`guide-peaking.html`** (meet peaking). Each article uses the shared `.guide-wrap`/`.guide-crumbs`/`.guide-eyebrow`/`.guide-title`(Anton)/`.guide-standfirst`/`.guide-meta`/`.guide-toc`(numbered, decimal-leading-zero red `::before` counters)/`.prose`(h2 red-bar `::before`, p, `.guide-formula`+`.fx-note`, `.guide-callout`+`.gc-icon`/`.gc-body`, ul/ol)/`.guide-cta`/`.guide-related`+`.guide-rel-card` structure — **all `.guide-*`/`.prose` CSS lives in styles.css**. **The math in every article mirrors `app.js` exactly** (e1RM `weight ÷ (1 − ((reps−1)+(10−RPE))×0.0333)` floored at `weight÷0.4`; the full `VARIANTS` multiplier tables; the `dotsScore` denom polynomial + men/women coefficients; peaking attempt %s) — keep them in sync if the formulas change. **SEO:** each article carries two JSON-LD blocks (`Article` + `BreadcrumbList`), unique title/description/og/twitter/canonical meta; **`sitemap.xml` rewritten to 13 URLs** (added the hub + 4 guides + the previously-missing `exercises.html` + `leaderboard.html`); **every public-page footer now cross-links "Guides"** (+ "Exercises" where it was absent) — index, marketplace, about, faq, privacy, terms, leaderboard, coach-profile, exercises. `renderNav` is action-based (not a link list), so these pages surface via footer + cross-links, not top nav. All `bootstrap(null, …, { staticPublic: true })`. Verified at 1280 + 375 (hub grid, article nav/breadcrumb/Anton headings/numbered TOC/prose/formula blocks — no horizontal overflow at mobile, formula blocks wrap), 0 console errors on all 5 pages, all 12 internal links resolve 200. No app.js change → tests.html unchanged (137 green). **Hook false positives (left as-is, no inline ignores):** `single-font` fires because the scanner only sees the line-30 Google Fonts `<link>` (the three brand families are applied via styles.css classes); `numbered-section-markers` fires on the legitimate `.guide-toc` ordered list (a real table-of-contents sequence — the h2s use red bars, not numbers).
- **First-run onboarding + empty states** — DONE. The **guided-tour engine already existed** (`startTour`/`renderTourStep`/`isTourDone`/`resetTour`/`endTour` in app.js ~805-934; localStorage `powa-tour-<name>-done`; auto-start + replay on both dashboards — athlete.html `maybeStartAthleteTour` [6 steps] + `window.replayAthleteTour`, coach.html `startTour('coach', …)` + `window.replayCoachTour`), so onboarding needed no work. **Empty-state coverage was already broad** (shared `.empty` icon+h3+p+actions pattern across athlete Today/Full-program/Bodyweight/Library/Marketplace + Coach Roster/Builder/Templates/Listings/Progress/Messages). The **one real first-run gap: the athlete Progress tab** rendered a wall of zero-stats + empty charts when `logs.length === 0`. Fixed by leading `renderProgress()` (athlete.html) with a first-run `.empty` state (bar icon, ALL-CAPS Anton "Your progress starts with one set", sentence-case guidance, red primary **"Log your first set →"** → `tab-today` + ghost **"Import history"** → `openImportModal`) and early-returning, replacing the zeros with one clear path. Athlete-only, demo-safe, additive; no pure helper → tests.html unchanged (137 green). Verified at 1280 + 375 (renders as sole child of `#progressArea`, on-brand, 0 console errors). Coach notes/form-check inboxes are nested in the roster render, which already carries the "No athletes yet" empty state, so they're correctly absent (not blank) for a rosterless coach.
- **Weekly progress email digest** — DONE (PREP). New `email_preferences` table (`migration-email-preferences.sql`): `weekly_digest` + `product_emails` booleans (both DEFAULT true), a UNIQUE `unsub_token` uuid, own-row RLS, a `touch` trigger, and a profiles backfill. **One table backs both this (#19) and the #36 one-click unsubscribe** (the token + `product_emails` are #36's groundwork). db.js: `mapDbEmailPrefs` (missing row = opted-in defaults) + `DB.getEmailPrefs(userId)` + `DB.updateEmailPrefs(userId, patch)` (upsert onConflict `user_id`). Client surface: an **"Email" section in the shared Settings modal** (`renderEmailPrefs` in app.js, mounted on `#setEmailPrefsBox` in `ensureSettingsModal`/`openSettingsModal`, right after the referral card) — two `.email-pref-row` toggles (red `accent-color`) that call `updateEmailPrefs` on change + toast, revert-on-failure; demo-safe (renders defaults, no network, "preview only" note). The row is a `<label>` so it overrides the modal's uppercase-label convention back to sentence case (`text-transform:none` on `.email-pref-row` in styles.css). Server: `supabase/functions/send-weekly-digest/index.ts` (see Edge functions) — cron batch, aggregates stored `e1rm_comp`, `CRON_SECRET`-gated, Verify JWT OFF (in `deploy.sh`'s `NO_JWT` group). **PREP: no email fires until deployed + `CRON_SECRET`/`RESEND_API_KEY` set + a Mondays-14:00-UTC pg_cron entry added** (snippet in the fn header; `.env.example` documents `CRON_SECRET`/`UNSUB_BASE`). Verified the Settings toggles at 1280 + 375 in demo mode (2 rows, sentence-case, red checkboxes, no overflow, toggle fires "preview only" toast). No new pure helper → tests.html unchanged (137 green).
- **Apple Health / Google Fit bodyweight sync** — SPIKE (design doc only, no code). `docs/health-sync-spike.md`. Bottom line: a no-build vanilla PWA **cannot** read HealthKit / Health Connect (both native-only); the Google Fit REST API is being shut down (2026) so it's a dead end. The Bodyweight tab already covers the 80% (manual `logBodyweight()` + the `openImportModal`/`_impBuild` CSV/paste import, which ingests `date,weight` rows — Apple Health's own "Export All Health Data" → CSV feeds it unchanged). Recommended automatic path is tiered and **forward-compatible**: **Tier 1** (web-possible today, no App Store) = an iOS **Shortcut** that reads Body Mass and POSTs to a new `ingest-bodyweight` edge fn gated on a per-user `profiles.bw_sync_token` (same fail-closed model as the digest's `CRON_SECRET`); **Tier 2** = first-class HealthKit/Health Connect once the native shell (#32) exists, reusing Tier 1's endpoint + `bodyweight` upsert. Kept a spike (not PREP) because Tier 1 is secret- **and** deploy-gated end-to-end (new fn + new `profiles` column migration + a token UI meaningless until both land); the doc carries the full promote-to-PREP checklist.
- **kg / lb unit toggle** — DONE (verified complete, no code change needed this pass). The system in app.js is comprehensive: `getUnit`/`setUnit`/`toggleUnit` (persisted per-device in `localStorage['powa-unit']`, toggle hard-reloads), `kgToDisplay`/`displayToKg` (bidirectional, `KG_PER_LB=0.45359237`), `unitLabel`/`formatWeight`. The **toggle button lives in `renderNav`**, so it's on every page (dashboards + public). Storage is **always kg**; display converts. Audited coverage: every weight **display** goes through `kgToDisplay`/`formatWeight` (athlete.html ~69 sites, coach.html ~22, leaderboard totals via `formatWeight` in `leaderboardRowsHtml`); every weight **input** converts back via `displayToKg` — bodyweight log (`logBodyweight` 4303), goals save (2378), meet results (`saveMeetResult`/`toKg` 1999), program-builder set weights (coach 1444/1539, athlete self-coached 4132), coach publish-1RM (`.pub1rm-unit` labels + `displayToKg` on save), and the CSV import modal has its own explicit unit `<select>`. **Weight classes intentionally stay in kg** (IPF/USAPL classes are defined in kg — correct not to convert). marketplace.html / coach-profile.html show only prices ($, that's #28) — no raw-kg weight leaks (the `migration-marketplace-1rm` column isn't surfaced in any render path). Live-verified the round-trip in demo: 200 kg → "440.9 lbs", 441 lb input → 200 kg stored, label follows the mode.
- **Marketplace currency localization** — DONE. Programs are stored + charged in **USD cents** and **Lemon Squeezy is Merchant of Record** — LS shows each buyer their exact localized total at checkout — so the on-site figure is a USD *reference* price. New shared `fmtPrice(cents, opts)` in app.js (next to `fmtMoneyCents`, exported on `window`) formats that reference through the **viewer's own locale** via `Intl.NumberFormat({style:'currency',currency:'USD'})`: localized grouping (`1,299` / `1.299` / `1 299`) and — the actual point — most non-US locales render USD as **"US$…" / "USD …" / "$US"**, disambiguating it from the viewer's local dollar (AUD/CAD/NZD). **Pure `Intl` — no FX rate table (nothing to go stale), no network, no secret, demo-safe**; graceful `$X` fallback if `Intl`/currency is unavailable. `marketplace.html`'s `fmtUsd` now delegates to `fmtPrice` (falls back to its old inline formatter if app.js hasn't loaded), so both the browse-card price (`.mk-card-price`) and the detail buy-panel price (`.mk-buy-price`) are locale-aware. Added a muted `.mk-buy-cur` clarity line under the buy price — *"Priced in USD · your local total is shown at checkout"* — honest about LS handling the real conversion. **Coach-side stays USD** on purpose (coaches set USD prices + earn USD payouts; `fmtMoneyCents` for earnings is unchanged). No migration (display-only). Live-verified: en-US → `$29.99`, en-GB → `US$29.99`, en-AU → `USD 39.99`, fr-FR → `29,99 $US`, de-DE grouping `1.299 $`; 0 console errors, buy card clean at 1280 + 375.
- **Push notification triggers + per-category prefs** — DONE (PREP). The Web Push *infra* already existed (`PowaPush` in app.js, `VAPID_PUBLIC_KEY` set, `push_subscriptions` + `send-push` edge fn, athlete device toggle) but **nothing real ever fired a push** (only the "send a test" button) and there were **no per-category preferences** and **no coach-side control**. This closes all three. **(1) Three real triggers, all fire-and-forget so they never block the action or throw into the UI:** a new **message** → push to the other party (`category:'messages'`, deep-links to their dashboard, title "New message from <first name>") in `renderMessageThread`'s `doSend` (app.js, non-demo branch only); a **form-check reply** → push to the athlete (`category:'form_checks'`, title "<coach> replied to your form check") in coach.html's reply handler; a **program assigned** ("📧 Notify" button) → push to the athlete (`category:'program'`) in `notifyAthleteAssigned`, fired **independent of the email path so athletes with no email on file still get it**. **(2) Per-category opt-outs** live on the existing `email_preferences` table via 3 new bool cols (`migration-notification-prefs.sql`, default true = opted in) — chosen over a new table since it's already own-row RLS'd + upsert-shaped. `send-push` reads the RECIPIENT's row **server-side** (service role) and skips a delivery whose category is switched off; the sender's browser can't see the recipient's prefs, so muting *must* be enforced there. **(3) Shared Settings UI** — new `renderNotifPrefs(container)` in app.js renders a **device control** (this browser's subscription via `PowaPush.enable/disable/test`, with unsupported + `denied` states handled) + **three category toggles** (Messages / Form-check replies / Program updates) into a `setNotifPrefsBox` card in the shared Settings modal, so **both** dashboards get it (coach had none before). **Every layer FAILS OPEN / is pre-migration-safe:** `mapDbEmailPrefs` defaults the 3 push fields true; `DB.updateEmailPrefs` retries without the push cols on a column-missing error; `send-push` wraps the pref lookup in try/catch and sends on any error — so the code is correct whether or not the migration + redeploy have landed. db.js: `mapDbEmailPrefs` +3 push fields, `updateEmailPrefs` +push keys (with column-missing retry), `sendPush` +`category`. send-push/index.ts: +`category` clip + `CATEGORY_COL` recipient fail-open pref check. Demo-safe (preview-only branch in `renderNotifPrefs`; triggers are all in non-demo paths). Live-verified: card renders with all 3 toggles on athlete **and** coach dashboards, 0 console errors, no overflow at 1280 + 375. **To bring fully live:** run `migration-notification-prefs.sql` + redeploy `send-push` (both optional thanks to fail-open) — the push *delivery* itself still needs the VAPID keypair set as `send-push` secrets, same as before.
- **Native iOS / Android app** — SPIKE (design doc + inert scaffold templates only, no native code, no tooling installed). `docs/native-app-spike.md`. Decision: **wrap the existing PWA in a thin native shell with [Capacitor](https://capacitorjs.com)** — the web app ships verbatim (Capacitor loads the *same* HTML/JS/CSS in a native WebView), so **zero rewrite, no build step, one codebase**. Rejected: React Native / Flutter (full ~7k-line rewrite into a new framework, violates vanilla-JS rule), pure PWA (no iOS App Store presence), TWA (Android-only, no iOS equivalent). Foundation is already done — complete `manifest.json` (maskable icons), network-first `sw.js`, Web Push (VAPID), 375px-verified responsive UI, `og.png`+icon PNGs for asset generation. Phased plan in the doc: **A** scaffold (isolated `native/` workspace, `webDir: ..` bundles the static files → app opens offline; `native/node_modules`+`ios`+`android` gitignored, only `capacitor.config.json` is source) / **B** native push (APNs+FCM via `@capacitor/push-notifications`, add a `platform` col to `push_subscriptions` + branch `send-push` — Web Push for `web` rows, native tokens otherwise; the per-category prefs above carry over) / **C** native plugins (HealthKit / Health Connect unlocks #23 Tier 2 reusing its endpoint; Camera for form-check capture; Haptics/StatusBar/SplashScreen branding) / **D** store submission (`@capacitor/assets` for icons/splash, reuse `privacy.html`'s data map for nutrition labels). Kept a spike (not PREP) because **everything past scaffold is account- + tooling-gated outside the repo**: Xcode + Android Studio, **Apple Developer $99/yr** + **Play Console $25**, signing certs, APNs/FCM credentials, store review — and **Apple guideline 4.2 rejects thin web wrappers**, so v1 must ship Phase B+C native value (push/health/camera/offline), not a bare WebView. The doc carries the promote-to-build checklist + inert `capacitor.config.json` / `.gitignore` templates.

## Hero coach-pitch (new this session)

In `index.html` between the 3 hero CTAs and the demo line:

```html
<p class="hero-coach-pitch" aria-label="Pitch for coaches" data-fx="up">
  <span class="hcp-label">FOR COACHES</span>
  <span class="hcp-body">Free with your first 3 athletes. Sell programs on the marketplace. Stop spreadsheeting. <a href="#pricing" class="hcp-link">See pricing →</a></span>
</p>
```

Styled in `ui-fx.css`. Anchor-jumps to `#pricing`. Red border + gradient bg + hover lift.

## Marketing assets generated this session

- `og.png` 1200x630 — Open Graph image (referenced in all page meta).
- `icon-192.png`, `icon-512.png`, `icon-512-maskable.png` — PWA icons referenced by `manifest.json`.
- Plus some Higgsfield abstract images and Instagram launch assets that weren't downloaded into the repo (sandbox limitation). They live in the Higgsfield workspace if you want to grab them.

## Style conventions

- Inline `<script>` blocks at the bottom of each HTML page wire up the page-specific glue. `app.js` is the shared library.
- Don't introduce a build step. Vercel serves the files raw.
- Don't add new JS frameworks. Pure vanilla.
- Animation modules use the `fx-*` class prefix.
- All new tables/policies should be in their own `migration-*.sql` file at the repo root for the user to run manually.

## When in doubt

- Editing the hero markup: also update the inline `data-fx` attributes so scroll-fx still fires correctly.
- Adding a new feature that touches DB: write a migration + check if you need to add a mapper in `db.js`.
- Adding a new public page: include `styles.css`, `ui-fx.css`, `insane-fx.css`, `error-log.js`, and the Plausible tag, plus the standard nav + footer.
