{
  "name": "powalifta-native",
  "version": "1.0.0",
  "private": true,
  "description": "Native iOS/Android shell for POWALIFTA (Capacitor). Wraps the existing web app and adds a native rest-timer Live Activity / Dynamic Island countdown. Does NOT affect the Vercel-served web app — this folder is self-contained.",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios",
    "add:ios": "cap add ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
